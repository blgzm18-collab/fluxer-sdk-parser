import { Snowflake } from '@fluxerjs/types';

/**
 * Multi-flag storage backed by bigint (supports bits ≥ 32).
 */
type BitFieldResolvable<S extends string> = S | bigint | number | string | BitField<S> | readonly (S | number | bigint | string | BitField<S>)[];
declare class BitField<S extends string> {
    static Flags: Record<string, bigint>;
    static DefaultBit: bigint;
    bitfield: bigint;
    constructor(bits?: BitFieldResolvable<S>);
    get [Symbol.toStringTag](): string;
    /** Resolve a resolvable into a single bigint bitfield. */
    static resolve<S extends string>(bits: BitFieldResolvable<S>): bigint;
    private get ctor();
    private orAll;
    /** Whether this bitfield contains all of the given bits. */
    has(bit: BitFieldResolvable<S>): boolean;
    /** Whether this bitfield contains any of the given bits. */
    any(bit: BitFieldResolvable<S>): boolean;
    /** Bits present in `bit` but missing from this bitfield. */
    missing(bit: BitFieldResolvable<S>): S[];
    add(...bits: BitFieldResolvable<S>[]): this;
    remove(...bits: BitFieldResolvable<S>[]): this;
    serialize(): Record<S, boolean>;
    toArray(): S[];
    /** Decimal string of the bigint bitfield (API / JSON). */
    toJSON(): string;
    toString(): string;
    valueOf(): bigint;
    equals(other: BitFieldResolvable<S>): boolean;
    freeze(): Readonly<this>;
    [Symbol.iterator](): IterableIterator<S>;
}

/**
 * Get the Unicode character for a known shortcode name.
 * Case-insensitive. Returns undefined if the shortcode is not in the map.
 */
declare function getUnicodeFromShortcode(name: string): string | undefined;

/**
 * Stable machine-readable error codes for {@link FluxerError}.
 * Keys are PascalCase; values are SCREAMING_SNAKE.
 */
declare const ErrorCodes: {
    readonly ClientNotReady: "CLIENT_NOT_READY";
    readonly NotLoggedIn: "NOT_LOGGED_IN";
    readonly InvalidToken: "INVALID_TOKEN";
    readonly AlreadyLoggedIn: "ALREADY_LOGGED_IN";
    readonly ChannelNotFound: "CHANNEL_NOT_FOUND";
    readonly MessageNotFound: "MESSAGE_NOT_FOUND";
    readonly GuildNotFound: "GUILD_NOT_FOUND";
    readonly MemberNotFound: "MEMBER_NOT_FOUND";
    readonly RoleNotFound: "ROLE_NOT_FOUND";
    readonly EmojiNotInGuild: "EMOJI_NOT_IN_GUILD";
    readonly EmojiNotFound: "EMOJI_NOT_FOUND";
    readonly InvalidEmoji: "INVALID_EMOJI";
    readonly EmojiRequiresGuild: "EMOJI_REQUIRES_GUILD";
    readonly WebhookTokenRequired: "WEBHOOK_TOKEN_REQUIRED";
    readonly InvalidInvite: "INVALID_INVITE";
    readonly InvalidFileUrl: "INVALID_FILE_URL";
    readonly FileFetchFailed: "FILE_FETCH_FAILED";
    readonly InvalidAttachment: "INVALID_ATTACHMENT";
    readonly WebSocketLoadFailed: "WEBSOCKET_LOAD_FAILED";
    readonly GatewayConnectionAborted: "GATEWAY_CONNECTION_ABORTED";
    readonly GatewayFetchFailed: "GATEWAY_FETCH_FAILED";
    readonly AttachmentFilenameRequired: "ATTACHMENT_FILENAME_REQUIRED";
    readonly InvalidEmbedMediaUrl: "INVALID_EMBED_MEDIA_URL";
    readonly InvalidEmbedUrl: "INVALID_EMBED_URL";
    readonly VoiceWebSocketRequired: "VOICE_WEBSOCKET_REQUIRED";
    readonly VoiceHttpError: "VOICE_HTTP_ERROR";
    readonly VoiceNoResponseBody: "VOICE_NO_RESPONSE_BODY";
    readonly InvalidInstanceDiscovery: "INVALID_INSTANCE_DISCOVERY";
    readonly ConflictingInstanceConfig: "CONFLICTING_INSTANCE_CONFIG";
    /** @beta ClientCluster: duplicate runtime id */
    readonly DuplicateRuntimeId: "DUPLICATE_RUNTIME_ID";
    /** @beta ClientCluster: unknown runtime id */
    readonly RuntimeNotFound: "RUNTIME_NOT_FOUND";
    /** @beta ClientCluster: concurrent conflicting op on same id */
    readonly RuntimeConflict: "RUNTIME_CONFLICT";
    /** @beta ClientCluster: cluster already destroyed */
    readonly ClusterDestroyed: "CLUSTER_DESTROYED";
    /** @beta ClientCluster: prebuilt client already logged in */
    readonly RuntimeAlreadyLoggedIn: "RUNTIME_ALREADY_LOGGED_IN";
    /** @beta ClientCluster: missing/empty runtime id or token */
    readonly InvalidRuntimeConfig: "INVALID_RUNTIME_CONFIG";
    readonly EmptyMessage: "EMPTY_MESSAGE";
    readonly InvalidMessageOptions: "INVALID_MESSAGE_OPTIONS";
    readonly InvalidBulkDelete: "INVALID_BULK_DELETE";
    /** Fetch / bulk-fetch limit or batch size out of range. */
    readonly InvalidFetchLimit: "INVALID_FETCH_LIMIT";
    /** Attachment plan/file mismatch or invalid attachment input. */
    readonly InvalidAttachmentInput: "INVALID_ATTACHMENT_INPUT";
    /** Presigned CDN PUT failed. */
    readonly AttachmentUploadFailed: "ATTACHMENT_UPLOAD_FAILED";
    /** Operation requires a personal-notes channel (or wrong channel type). */
    readonly InvalidChannelType: "INVALID_CHANNEL_TYPE";
    /** Gateway opcode payload missing required fields. */
    readonly InvalidGatewayRequest: "INVALID_GATEWAY_REQUEST";
};
type ErrorCode = (typeof ErrorCodes)[keyof typeof ErrorCodes];

interface FluxerErrorOptions {
    /** Machine-readable code from {@link ErrorCodes}. */
    code?: ErrorCode | string;
    cause?: unknown;
}
/**
 * Base error for Fluxer SDK failures.
 * Prefer an {@link ErrorCodes} value so callers can branch on `error.code`.
 */
declare class FluxerError extends Error {
    readonly code: string | undefined;
    constructor(message: string, options?: FluxerErrorOptions);
    static isFluxerError(error: unknown): error is FluxerError;
    toString(): string;
    toJSON(): {
        name: string;
        message: string;
        code?: string;
        cause?: unknown;
    };
}

/**
 * Format a number as a `#rrggbb` hex color string.
 */
declare function formatColor(color: number): string;
/**
 * Escape markdown formatting characters in a string.
 */
declare function escapeMarkdown(text: string): string;
type TimestampStyle = 't' | 'T' | 'd' | 'D' | 'f' | 'F' | 'R';
/**
 * Format a Unix timestamp (milliseconds) as a Fluxer/Discord timestamp mention.
 * @param timestampMs - Unix timestamp in milliseconds
 * @param style - Optional display style (`t`, `T`, `d`, `D`, `f`, `F`, `R`)
 */
declare function formatTimestamp(timestampMs: number, style?: TimestampStyle): string;
/**
 * Truncate a string to a maximum length with an optional suffix.
 * If `maxLength` is smaller than the suffix, returns the suffix truncated to `maxLength`.
 */
declare function truncate(str: string, maxLength: number, suffix?: string): string;

/** Result for embed media: url and optional flags. */
interface KlipyMediaResult {
    url: string;
    flags?: number;
}
/**
 * Resolve a Klipy view URL to a direct GIF URL for embeds.
 * Prefers JSON-LD / Open Graph from the page; falls back to oEmbed thumbnail → `.gif`.
 */
declare function resolveKlipyToImageUrl(klipyViewUrl: string): Promise<KlipyMediaResult | null>;

type LogLevel = 'error' | 'warn' | 'info' | 'debug';
type LogFields = Record<string, unknown>;
type LoggerSink = (level: LogLevel, message: string, fields?: LogFields) => void;
interface LoggerOptions {
    /** Minimum level to emit. Default: `warn`. */
    level?: LogLevel;
    /** Optional sink; defaults to console. */
    sink?: LoggerSink;
}
interface Logger {
    readonly level: LogLevel;
    error(message: string, fields?: LogFields): void;
    warn(message: string, fields?: LogFields): void;
    info(message: string, fields?: LogFields): void;
    debug(message: string, fields?: LogFields): void;
    child(fields: LogFields): Logger;
}
/** Walk `Error.cause` into a plain JSON-safe object. */
declare function serializeError(err: unknown, depth?: number): Record<string, unknown> | string;
declare function createLogger(options?: LoggerOptions): Logger;

/**
 * Message flag bits for {@link MessageFlagsBitField}.
 * Values mirror `@fluxerjs/types` {@link MessageFlags} (int32 wire bitfield) as bigint.
 */
declare const MessageFlagsBits: {
    readonly SuppressEmbeds: bigint;
    readonly SuppressNotifications: bigint;
    readonly VoiceMessage: bigint;
    readonly CompactAttachments: bigint;
};
type MessageFlagsString = keyof typeof MessageFlagsBits;
type MessageFlagsResolvable = BitFieldResolvable<MessageFlagsString>;
declare class MessageFlagsBitField extends BitField<MessageFlagsString> {
    static Flags: {
        readonly SuppressEmbeds: bigint;
        readonly SuppressNotifications: bigint;
        readonly VoiceMessage: bigint;
        readonly CompactAttachments: bigint;
    };
}

/**
 * Permission flags aligned with the Fluxer API (`1n << n`).
 * Administrator (bit 3) implies all permissions in {@link PermissionsBitField.has}.
 */
declare const PermissionFlags: {
    readonly CreateInstantInvite: bigint;
    readonly KickMembers: bigint;
    readonly BanMembers: bigint;
    readonly Administrator: bigint;
    readonly ManageChannels: bigint;
    readonly ManageGuild: bigint;
    readonly AddReactions: bigint;
    readonly ViewAuditLog: bigint;
    readonly PrioritySpeaker: bigint;
    readonly Stream: bigint;
    readonly ViewChannel: bigint;
    readonly SendMessages: bigint;
    readonly SendTtsMessages: bigint;
    readonly ManageMessages: bigint;
    readonly EmbedLinks: bigint;
    readonly AttachFiles: bigint;
    readonly ReadMessageHistory: bigint;
    readonly MentionEveryone: bigint;
    readonly UseExternalEmojis: bigint;
    readonly Connect: bigint;
    readonly Speak: bigint;
    readonly MuteMembers: bigint;
    readonly DeafenMembers: bigint;
    readonly MoveMembers: bigint;
    readonly UseVad: bigint;
    readonly ChangeNickname: bigint;
    readonly ManageNicknames: bigint;
    readonly ManageRoles: bigint;
    readonly ManageWebhooks: bigint;
    /** Manage emojis & stickers (expressions). */
    readonly ManageExpressions: bigint;
    readonly UseExternalStickers: bigint;
    readonly ModerateMembers: bigint;
    readonly CreateExpressions: bigint;
    readonly PinMessages: bigint;
    readonly BypassSlowmode: bigint;
    readonly UpdateRtcRegion: bigint;
    readonly ViewChannelMembers: bigint;
};
/** OR of all permission flags (guild owner / Administrator override). */
declare const ALL_PERMISSIONS_BIGINT: bigint;
type PermissionString = keyof typeof PermissionFlags;
type PermissionResolvable = BitFieldResolvable<PermissionString>;
declare class PermissionsBitField extends BitField<PermissionString> {
    static Flags: {
        readonly CreateInstantInvite: bigint;
        readonly KickMembers: bigint;
        readonly BanMembers: bigint;
        readonly Administrator: bigint;
        readonly ManageChannels: bigint;
        readonly ManageGuild: bigint;
        readonly AddReactions: bigint;
        readonly ViewAuditLog: bigint;
        readonly PrioritySpeaker: bigint;
        readonly Stream: bigint;
        readonly ViewChannel: bigint;
        readonly SendMessages: bigint;
        readonly SendTtsMessages: bigint;
        readonly ManageMessages: bigint;
        readonly EmbedLinks: bigint;
        readonly AttachFiles: bigint;
        readonly ReadMessageHistory: bigint;
        readonly MentionEveryone: bigint;
        readonly UseExternalEmojis: bigint;
        readonly Connect: bigint;
        readonly Speak: bigint;
        readonly MuteMembers: bigint;
        readonly DeafenMembers: bigint;
        readonly MoveMembers: bigint;
        readonly UseVad: bigint;
        readonly ChangeNickname: bigint;
        readonly ManageNicknames: bigint;
        readonly ManageRoles: bigint;
        readonly ManageWebhooks: bigint;
        /** Manage emojis & stickers (expressions). */
        readonly ManageExpressions: bigint;
        readonly UseExternalStickers: bigint;
        readonly ModerateMembers: bigint;
        readonly CreateExpressions: bigint;
        readonly PinMessages: bigint;
        readonly BypassSlowmode: bigint;
        readonly UpdateRtcRegion: bigint;
        readonly ViewChannelMembers: bigint;
    };
    private get isAdmin();
    /** Administrator implies every permission. */
    has(bit: PermissionResolvable): boolean;
    any(bit: PermissionResolvable): boolean;
    missing(bit: PermissionResolvable): PermissionString[];
}
/** Resolve permission(s) to an API bitfield string (decimal bigint). */
declare function resolvePermissionsToBitfield(perms: PermissionResolvable): string;

/** Narrow unknown values to plain objects (not arrays / null). */
declare function isRecord(value: unknown): value is Record<string, unknown>;
/** Same as {@link isRecord}, or `null` when the value is not a record. */
declare function asRecord(value: unknown): Record<string, unknown> | null;
declare function str(value: unknown): string | undefined;
declare function num(value: unknown): number | undefined;

/**
 * Resolve a color from various input types to a number (0–16777215).
 */
declare function resolveColor(color: number | string | readonly [number, number, number]): number;
interface ParsedEmoji {
    id: string | null;
    name: string;
    animated?: boolean;
}
/**
 * Parse an emoji string into id and name.
 * Supports: `<a?:name:id>`, `:name:`, `name:id`, unicode.
 */
declare function parseEmoji(emoji: string): ParsedEmoji | null;
/**
 * Parse a user mention or raw snowflake and extract the user ID.
 * Supports: `<@id>`, `<@!id>`, or a valid snowflake string.
 */
declare function parseUserMention(arg: string): string | null;
/**
 * Parse a role mention (`<@&id>`) and extract the role ID.
 */
declare function parseRoleMention(arg: string): string | null;
interface ParsedPrefixCommand {
    command: string;
    args: string[];
}
/**
 * Parse prefix command content into command name and args.
 * Returns null if content does not start with the prefix.
 */
declare function parsePrefixCommand(content: string, prefix: string): ParsedPrefixCommand | null;
/**
 * Convert emoji to API reaction format.
 * Custom: `name:id` (animated: `a:name:id`); Unicode: raw character (routes encode).
 */
declare function formatEmoji(emoji: {
    id: string | null;
    name: string;
    animated?: boolean;
}): string;

/** Fluxer epoch (2015-01-01 00:00:00 UTC) in milliseconds. */
declare const FLUXER_EPOCH = 1420070400000n;
interface DeconstructedSnowflake {
    /** Unix timestamp in milliseconds. */
    timestamp: number;
    date: Date;
    workerId: number;
    processId: number;
    increment: number;
}
/**
 * Fluxer snowflake IDs (Twitter layout, custom epoch).
 * Public API is string-only — never number — to avoid precision loss above 2^53.
 */
declare class SnowflakeUtil {
    static readonly EPOCH = 1420070400000n;
    /** Whether `value` is a non-negative decimal snowflake string (≤ 20 digits). */
    static isValid(snowflake: string): boolean;
    /** Parse a snowflake string to bigint. */
    static parse(snowflake: Snowflake): bigint;
    /** Unix timestamp (ms) from a snowflake. */
    static timestampFromSnowflake(snowflake: Snowflake): number;
    /** Date from a snowflake. */
    static dateFromSnowflake(snowflake: Snowflake): Date;
    /**
     * Snowflake string from a Unix timestamp (ms).
     * Useful for pagination (`before` / `after`).
     */
    static snowflakeFromTimestamp(timestamp: number): Snowflake;
    /** Deconstruct a snowflake into timestamp / worker / process / increment. */
    static deconstruct(snowflake: Snowflake): DeconstructedSnowflake;
}

/**
 * User flag bits matching the API UserFlags schema (int64).
 * Stored as bigint so bits ≥ 32 work with {@link BitField} operations.
 */
declare const UserFlagsBits: {
    readonly Staff: bigint;
    readonly CtpMember: bigint;
    readonly Partner: bigint;
    readonly BugHunter: bigint;
    readonly FriendlyBot: bigint;
    readonly FriendlyBotManualApproval: bigint;
    readonly HighGlobalRateLimit: bigint;
    readonly Deleted: bigint;
    readonly DisabledSuspiciousActivity: bigint;
    readonly SelfDeleted: bigint;
    readonly PremiumDiscriminator: bigint;
    readonly Disabled: bigint;
    readonly HasSessionStarted: bigint;
    readonly PremiumBadgeHidden: bigint;
    readonly PremiumBadgeMasked: bigint;
    readonly PremiumBadgeTimestampHidden: bigint;
    readonly PremiumBadgeSequenceHidden: bigint;
    readonly PremiumPerksSanitized: bigint;
    readonly PremiumPurchaseDisabled: bigint;
    readonly PremiumEnabledOverride: bigint;
    readonly RateLimitBypass: bigint;
    readonly ReportBanned: bigint;
    readonly VerifiedNotUnderage: bigint;
    readonly PendingManualVerification: bigint;
    readonly HasDismissedPremiumOnboarding: bigint;
    readonly UsedMobileClient: bigint;
    readonly AppStoreReviewer: bigint;
    readonly DmHistoryBackfilled: bigint;
    readonly HasRelationshipsIndexed: bigint;
    readonly MessagesByAuthorBackfilled: bigint;
    readonly StaffHidden: bigint;
};
type UserFlagsString = keyof typeof UserFlagsBits;
type UserFlagsResolvable = BitFieldResolvable<UserFlagsString>;
declare class UserFlagsBitField extends BitField<UserFlagsString> {
    static Flags: {
        readonly Staff: bigint;
        readonly CtpMember: bigint;
        readonly Partner: bigint;
        readonly BugHunter: bigint;
        readonly FriendlyBot: bigint;
        readonly FriendlyBotManualApproval: bigint;
        readonly HighGlobalRateLimit: bigint;
        readonly Deleted: bigint;
        readonly DisabledSuspiciousActivity: bigint;
        readonly SelfDeleted: bigint;
        readonly PremiumDiscriminator: bigint;
        readonly Disabled: bigint;
        readonly HasSessionStarted: bigint;
        readonly PremiumBadgeHidden: bigint;
        readonly PremiumBadgeMasked: bigint;
        readonly PremiumBadgeTimestampHidden: bigint;
        readonly PremiumBadgeSequenceHidden: bigint;
        readonly PremiumPerksSanitized: bigint;
        readonly PremiumPurchaseDisabled: bigint;
        readonly PremiumEnabledOverride: bigint;
        readonly RateLimitBypass: bigint;
        readonly ReportBanned: bigint;
        readonly VerifiedNotUnderage: bigint;
        readonly PendingManualVerification: bigint;
        readonly HasDismissedPremiumOnboarding: bigint;
        readonly UsedMobileClient: bigint;
        readonly AppStoreReviewer: bigint;
        readonly DmHistoryBackfilled: bigint;
        readonly HasRelationshipsIndexed: bigint;
        readonly MessagesByAuthorBackfilled: bigint;
        readonly StaffHidden: bigint;
    };
}

export { ALL_PERMISSIONS_BIGINT, BitField, type BitFieldResolvable, type DeconstructedSnowflake, type ErrorCode, ErrorCodes, FLUXER_EPOCH, FluxerError, type FluxerErrorOptions, type KlipyMediaResult, type LogFields, type LogLevel, type Logger, type LoggerOptions, type LoggerSink, MessageFlagsBitField, MessageFlagsBits, type MessageFlagsResolvable, type MessageFlagsString, type ParsedEmoji, type ParsedPrefixCommand, PermissionFlags, type PermissionResolvable, type PermissionString, PermissionsBitField, SnowflakeUtil, type TimestampStyle, UserFlagsBitField, UserFlagsBits, type UserFlagsResolvable, type UserFlagsString, asRecord, createLogger, escapeMarkdown, formatColor, formatEmoji, formatTimestamp, getUnicodeFromShortcode, isRecord, num, parseEmoji, parsePrefixCommand, parseRoleMention, parseUserMention, resolveColor, resolveKlipyToImageUrl, resolvePermissionsToBitfield, serializeError, str, truncate };
