import { S as Snowflake } from './SubpathRoutes-BE1OkJ06.js';
export { F as FLUXER_EPOCH, R as Routes } from './SubpathRoutes-BE1OkJ06.js';

/**
 * Wire types for message embeds (OpenAPI / gateway payloads).
 *
 * Prefer {@link RESTPostAPIEmbed} when *sending* embeds — response embeds may
 * include video/audio/children that create requests do not accept.
 */
/** Author block shown above an embed title. */
interface APIEmbedAuthor {
    /** Display name. */
    name?: string;
    /** Clickable URL for the author name. */
    url?: string;
    /** Author avatar image URL. */
    icon_url?: string;
    /** CDN proxy URL for `icon_url`. */
    proxy_icon_url?: string;
}
/** Footer text/icon at the bottom of an embed. */
interface APIEmbedFooter {
    /** Footer text (required when a footer is present). */
    text: string;
    /** Small icon beside the footer text. */
    icon_url?: string;
    /** CDN proxy URL for `icon_url`. */
    proxy_icon_url?: string;
}
/** Bitwise flags for embed media. */
declare const EmbedMediaFlags: {
    /** Embed media contains explicit content */
    readonly CONTAINS_EXPLICIT_MEDIA: 16;
    /** Embed media is animated */
    readonly IS_ANIMATED: 32;
};
/** Image, thumbnail, video, or audio media attached to an embed. */
interface APIEmbedMedia {
    /** Source URL of the media. */
    url: string;
    /** CDN proxy URL for the media. */
    proxy_url?: string | null;
    /** MIME type when known. */
    content_type?: string | null;
    /** Content hash for cache busting / integrity. */
    content_hash?: string | null;
    /** Pixel width when known. */
    width?: number | null;
    /** Pixel height when known. */
    height?: number | null;
    /** Alt text / caption for accessibility. */
    description?: string | null;
    /** Base64 placeholder for lazy loading */
    placeholder?: string | null;
    /** Duration in seconds for video/audio. */
    duration?: number | null;
    /** EmbedMediaFlags bitfield (e.g. EmbedMediaFlags.CONTAINS_EXPLICIT_MEDIA) */
    flags?: number | null;
}
/** Named field row inside an embed. */
interface APIEmbedField {
    /** Field title. */
    name: string;
    /** Field body (markdown supported). */
    value: string;
    /** When true, fields may render side-by-side. */
    inline?: boolean;
}
/**
 * Discriminator for how an embed is rendered.
 *
 * - `rich` — standard bot/webhook embed (title, fields, color, …)
 * - `image` — primarily an image unfurl
 * - `video` — primarily a video unfurl
 * - `gifv` — animated GIF / GIFV unfurl
 * - `article` — article / link-preview style card
 * - `link` — generic URL unfurl
 */
type EmbedType = 'rich' | 'image' | 'video' | 'gifv' | 'article' | 'link';
/** Nested embed from unfurlers (subset of {@link APIEmbed}). */
interface APIEmbedChild {
    /** Render kind; see {@link EmbedType}. */
    type?: EmbedType;
    /** Canonical URL associated with the embed. */
    url?: string | null;
    /** Title text. */
    title?: string | null;
    /** Accent color as a 24-bit integer (0xRRGGBB). */
    color?: number | null;
    /** ISO-8601 timestamp shown in the embed. */
    timestamp?: string | null;
    /** Body description (markdown). */
    description?: string | null;
    /** Author block. */
    author?: APIEmbedAuthor | null;
    /** Large image. */
    image?: APIEmbedMedia | null;
    /** Small thumbnail. */
    thumbnail?: APIEmbedMedia | null;
    /** Footer block. */
    footer?: APIEmbedFooter | null;
    /** Field rows. */
    fields?: APIEmbedField[] | null;
    /** Provider / site name block (same shape as author). */
    provider?: APIEmbedAuthor | null;
    /** Video media (response/unfurl only). */
    video?: APIEmbedMedia | null;
    /** Audio media (response/unfurl only). */
    audio?: APIEmbedMedia | null;
    /** Whether the embed is marked NSFW. */
    nsfw?: boolean | null;
}
/** Full embed object as returned by the API / gateway. */
interface APIEmbed {
    /** Render kind; see {@link EmbedType}. */
    type: EmbedType;
    /** Canonical URL associated with the embed. */
    url?: string | null;
    /** Title text. */
    title?: string | null;
    /** Accent color as a 24-bit integer (0xRRGGBB). */
    color?: number | null;
    /** ISO-8601 timestamp shown in the embed. */
    timestamp?: string | null;
    /** Body description (markdown). */
    description?: string | null;
    /** Author block. */
    author?: APIEmbedAuthor | null;
    /** Large image. */
    image?: APIEmbedMedia | null;
    /** Small thumbnail. */
    thumbnail?: APIEmbedMedia | null;
    /** Footer block. */
    footer?: APIEmbedFooter | null;
    /** Field rows. */
    fields?: APIEmbedField[] | null;
    /** Provider / site name block (same shape as author). */
    provider?: APIEmbedAuthor | null;
    /** Video media (response/unfurl only — not on create). */
    video?: APIEmbedMedia | null;
    /** Audio media (response/unfurl only — not on create). */
    audio?: APIEmbedMedia | null;
    /** Whether the embed is marked NSFW. */
    nsfw?: boolean | null;
    /** Nested embeds from unfurlers */
    children?: APIEmbedChild[] | null;
    /** Sanitized oEmbed HTML for specialized renderers. */
    html?: string | null;
    /** Preferred width of sanitized oEmbed HTML. */
    html_width?: number | null;
    /** Preferred height of sanitized oEmbed HTML. */
    html_height?: number | null;
}
/** Embed author for send/create requests. */
type RESTPostAPIEmbedAuthor = APIEmbedAuthor;
/** Embed footer for send/create requests. */
type RESTPostAPIEmbedFooter = APIEmbedFooter;
/** Embed media for send/create requests (URL + optional description only). */
type RESTPostAPIEmbedMedia = Pick<APIEmbedMedia, 'url' | 'description'>;
/**
 * Embed payload for message/webhook create endpoints.
 * Omits response-only fields (`video`, `audio`, `children`, `type`, …).
 */
interface RESTPostAPIEmbed {
    /** Canonical URL associated with the embed. */
    url?: string | null;
    /** Title text. */
    title?: string | null;
    /** Accent color as a 24-bit integer (0xRRGGBB). */
    color?: number | null;
    /** ISO-8601 timestamp shown in the embed. */
    timestamp?: string | null;
    /** Body description (markdown). */
    description: string | null;
    /** Author block. */
    author?: RESTPostAPIEmbedAuthor | null;
    /** Large image. */
    image?: RESTPostAPIEmbedMedia | null;
    /** Small thumbnail. */
    thumbnail?: RESTPostAPIEmbedMedia | null;
    /** Footer block. */
    footer?: RESTPostAPIEmbedFooter | null;
    /** Field rows. */
    fields?: APIEmbedField[] | null;
}

/**
 * Public user flags bitfield (OpenAPI PublicUserFlags).
 * - `Staff` — Fluxer staff member
 * - `CtpMember` — Closed Testing Program participant
 * - `Partner` — Fluxer partner
 * - `BugHunter` — Bug hunter
 * - `FriendlyBot` — Approved bot account
 * - `FriendlyBotManualApproval` — Bot requires manual approval
 * - `Spammer` — Marked as spammer
 * Full account flags (including private bits) live in `@fluxerjs/util` UserFlagsBitField.
 */
declare const PublicUserFlags: {
    readonly Staff: 1;
    readonly CtpMember: 2;
    readonly Partner: 4;
    readonly BugHunter: 8;
    readonly FriendlyBot: 16;
    readonly FriendlyBotManualApproval: 32;
    readonly Spammer: 64;
};
/** Union of all valid {@link PublicUserFlags} values. */
type PublicUserFlagsValue = (typeof PublicUserFlags)[keyof typeof PublicUserFlags];
/**
 * Guild member profile flags (OpenAPI GuildMemberProfileFlags).
 * - `AvatarUnset` — member explicitly unset their guild avatar
 * - `BannerUnset` — member explicitly unset their guild banner
 */
declare const GuildMemberProfileFlags: {
    readonly AvatarUnset: 1;
    readonly BannerUnset: 2;
};
/** Union of all valid {@link GuildMemberProfileFlags} values. */
type GuildMemberProfileFlagsValue = (typeof GuildMemberProfileFlags)[keyof typeof GuildMemberProfileFlags];
/**
 * Relationship type (OpenAPI RelationshipTypes).
 * - `Friend` — mutual friend
 * - `Blocked` — blocked user
 * - `IncomingRequest` — received friend request
 * - `OutgoingRequest` — sent friend request
 */
declare enum RelationshipType {
    Friend = 1,
    Blocked = 2,
    IncomingRequest = 3,
    OutgoingRequest = 4
}
/**
 * Partial user object returned by the API (messages, members, webhooks, etc.).
 * @see GET /users/{id} - Returns id, username, discriminator, global_name, avatar, avatar_color, flags
 * @see GET /users/@me - Returns full user with bot, email, premium fields, etc.
 */
interface APIUserPartial {
    /** User ID. */
    id: Snowflake;
    /** Username. */
    username: string;
    /** Discriminator (4-digit tag). */
    discriminator: string;
    /** Display name. */
    global_name: string | null;
    /** Avatar hash. */
    avatar: string | null;
    /** RGB color as integer (e.g. 7577782) for profile decoration. */
    avatar_color: number | null;
    /** {@link PublicUserFlags} bitfield (public bits); private bits may also be set. */
    flags: number;
    /** Account-wide reply mention preference. */
    mention_flags?: number;
    /** Whether this is a bot account. */
    bot?: boolean;
    /** Whether this is an official system user. */
    system?: boolean;
}
/** Alias for {@link APIUserPartial} — user object from API responses. */
type APIUser = APIUserPartial;
/**
 * User profile sub-object from GET /users/{id}/profile.
 * @see https://docs.fluxer.app/api-reference
 */
interface APIUserProfile {
    /** User pronouns. */
    pronouns?: string | null;
    /** User bio. */
    bio?: string | null;
    /** Profile banner hash. */
    banner?: string | null;
    /** Accent color as RGB integer. */
    accent_color?: number | null;
    /** Banner color as RGB integer. */
    banner_color?: number | null;
    /** Theme preference. */
    theme?: string | null;
}
/**
 * Connected account from profile response (e.g. GitHub, Twitter, etc.).
 */
interface APIConnectedAccount {
    /** Account name. */
    name?: string | null;
    /** Service type (e.g. "github", "twitter"). */
    type?: string | null;
}
/**
 * Full profile response from GET /users/{id}/profile.
 * Optionally use ?guild_id=GUILD_ID for server-specific profile.
 */
interface APIProfileResponse {
    /** User profile sub-object. */
    user_profile?: APIUserProfile | null;
    /** Mutual guilds (deprecated, use mutual_guild_ids). */
    mutual_guilds?: Array<{
        id: Snowflake;
    }> | null;
    /** Mutual guild IDs. */
    mutual_guild_ids?: Snowflake[] | null;
    /** Connected accounts. */
    connected_accounts?: APIConnectedAccount[] | null;
}
/**
 * Guild member from GET /guilds/{guild_id}/members or GET /guilds/{guild_id}/members/{user_id}.
 */
interface APIGuildMember {
    /** User object for this member. */
    user: APIUserPartial;
    /** Guild nickname. */
    nick?: string | null;
    /** Guild-specific avatar hash. */
    avatar?: string | null;
    /** Guild-specific banner hash. */
    banner?: string | null;
    /** Guild-specific accent color as RGB integer. */
    accent_color?: number | null;
    /** Role IDs assigned to this member. */
    roles: Snowflake[];
    /** ISO-8601 timestamp when the member joined. */
    joined_at: string;
    /** Whether the member is server muted. */
    mute: boolean;
    /** Whether the member is server deafened. */
    deaf: boolean;
    /** ISO-8601 timestamp until the member is timed out. */
    communication_disabled_until?: string | null;
    /** {@link GuildMemberProfileFlags} bitfield. */
    profile_flags?: number | null;
    /** Per-guild reply mention preference override. */
    mention_flags?: number | null;
}

/**
 * Message type discriminator (OpenAPI MessageType).
 * - `Default` — standard user/bot message
 * - `RecipientAdd` — DM group recipient added
 * - `RecipientRemove` — DM group recipient removed
 * - `Call` — voice/video call
 * - `ChannelNameChange` — channel name updated
 * - `ChannelIconChange` — channel icon updated
 * - `ChannelPinnedMessage` — message pinned
 * - `UserJoin` — member joined guild
 * - `Reply` — reply to another message
 */
declare enum MessageType {
    Default = 0,
    RecipientAdd = 1,
    RecipientRemove = 2,
    Call = 3,
    ChannelNameChange = 4,
    ChannelIconChange = 5,
    ChannelPinnedMessage = 6,
    UserJoin = 7,
    Reply = 19,
    /** Client-only system message (Fluxer `CLIENT_SYSTEM`). */
    ClientSystem = 99
}
/**
 * Message reference kind (OpenAPI MessageReferenceType).
 * - `Default` — reply to a message
 * - `Forward` — forwarded message
 */
declare enum MessageReferenceType {
    Default = 0,
    Forward = 1
}
/**
 * Message flags bitfield (OpenAPI MessageFlags).
 * - `SuppressEmbeds` — hide embeds in this message
 * - `SuppressNotifications` — silent message (no ping)
 * - `VoiceMessage` — message contains voice memo
 * - `CompactAttachments` — render attachments in compact grid
 * Use with {@link MessageFlagsBitField} in `@fluxerjs/util` for composition.
 */
declare const MessageFlags: {
    readonly SuppressEmbeds: 4;
    readonly SuppressNotifications: 4096;
    readonly VoiceMessage: 8192;
    readonly CompactAttachments: 131072;
};
/** Union of all valid {@link MessageFlags} values. */
type MessageFlagsValue = (typeof MessageFlags)[keyof typeof MessageFlags];
/**
 * Mention types parsed from message content when sending.
 * - `users` — parse @mentions
 * - `roles` — parse @role mentions
 * - `everyone` — parse @everyone/@here
 */
type AllowedMentionType = 'users' | 'roles' | 'everyone';
/** Controls which mentions trigger notifications in POST/PATCH message payloads. */
interface APIAllowedMentions {
    /** Which mention types to parse (`users`, `roles`, `everyone`). */
    parse?: AllowedMentionType[];
    /** Specific user IDs to mention. */
    users?: Snowflake[];
    /** Specific role IDs to mention. */
    roles?: Snowflake[];
    /** Whether to mention the author of the replied-to message. */
    replied_user?: boolean;
}
/** Emoji reference in a reaction. */
interface APIReactionEmoji {
    /** Custom emoji ID, or null for Unicode emoji. */
    id: Snowflake | null;
    /** Emoji name (Unicode char or custom emoji name). */
    name: string;
    /** Whether the emoji is animated. */
    animated?: boolean | null;
}
/** Reaction aggregation on a message. */
interface APIMessageReaction {
    /** The emoji used. */
    emoji: APIReactionEmoji;
    /** Total count of reactions with this emoji. */
    count: number;
    /** Whether the current user reacted with this emoji. */
    me?: boolean | null;
}
/** Reply/forward reference in GET /channels/{id}/messages. */
interface APIMessageReference {
    /** Channel containing the referenced message. */
    channel_id: Snowflake;
    /** ID of the referenced message. */
    message_id: Snowflake;
    /** Guild containing the referenced message (if guild message). */
    guild_id?: Snowflake | null;
    /** {@link MessageReferenceType.Default} = reply, {@link MessageReferenceType.Forward} = forward */
    type?: MessageReferenceType;
    /** Forward: attachment IDs to include when forwarding selected media */
    attachment_ids?: Snowflake[];
    /** Forward: embed indices to include when forwarding selected media */
    embed_indices?: number[];
}
/** Call metadata for call-type messages. */
interface APIMessageCall {
    /** Participant user IDs. */
    participants: string[];
    /** ISO-8601 timestamp when call ended. */
    ended_timestamp?: string | null;
}
/** Snapshot of a forwarded message's content and metadata. */
interface APIMessageSnapshot {
    /** Message text content. */
    content?: string | null;
    /** ISO-8601 timestamp when the original message was sent. */
    timestamp: string;
    /** ISO-8601 timestamp when the original message was last edited. */
    edited_timestamp?: string | null;
    /** User IDs mentioned in the original message. */
    mentions?: string[] | null;
    /** Role IDs mentioned in the original message. */
    mention_roles?: Snowflake[] | null;
    /** Embeds from the original message. */
    embeds?: APIEmbed[] | null;
    /** Attachments from the original message. */
    attachments?: APIMessageAttachment[] | null;
    /** Stickers from the original message. */
    stickers?: APIMessageSticker[] | null;
    /** Original message type. */
    type?: MessageType;
}
/**
 * Bitwise flags for message attachments (OpenAPI MessageAttachmentFlags).
 * - `IS_SPOILER` — renders attachment as spoiler (blurred until clicked)
 * - `CONTAINS_EXPLICIT_MEDIA` — attachment contains explicit content
 * - `IS_ANIMATED` — animated image (GIF, animated WebP), renders with EmbedGif / looping
 */
declare const MessageAttachmentFlags: {
    readonly IS_SPOILER: 8;
    readonly CONTAINS_EXPLICIT_MEDIA: 16;
    readonly IS_ANIMATED: 32;
};
/** File attachment on a message. */
interface APIMessageAttachment {
    /** Attachment ID (snowflake). */
    id: Snowflake;
    /** Original filename. */
    filename: string;
    /** Display title (if different from filename). */
    title?: string | null;
    /** Alt text / caption for accessibility. */
    description?: string | null;
    /** MIME type when known. */
    content_type?: string | null;
    /** Hash of attachment content for integrity checks. */
    content_hash?: string | null;
    /** File size in bytes. */
    size: number;
    /** CDN URL to download the file. */
    url?: string | null;
    /** CDN proxy URL. */
    proxy_url?: string | null;
    /** Pixel width for images/videos. */
    width?: number | null;
    /** Pixel height for images/videos. */
    height?: number | null;
    /** Base64 placeholder for lazy loading. */
    placeholder?: string | null;
    /** MessageAttachmentFlags bitfield (e.g. IS_SPOILER). */
    flags?: number | null;
    /** Whether the attachment is NSFW. */
    nsfw?: boolean | null;
    /** Duration in seconds for audio/video. */
    duration?: number | null;
    /** Base64 audio waveform for voice messages. */
    waveform?: string | null;
    /** ISO-8601 timestamp when the attachment URL expires. */
    expires_at?: string | null;
    /** Whether the attachment URL has expired. */
    expired?: boolean | null;
}
/** Sticker reference in a message. */
interface APIMessageSticker {
    /** Sticker ID. */
    id: Snowflake;
    /** Sticker name. */
    name: string;
    /** Sticker description. */
    description?: string;
    /** Sticker tags. */
    tags?: string[];
    /** Whether the sticker is animated. */
    animated?: boolean;
}
/**
 * Full message object from GET /channels/{id}/messages, POST /channels/{id}/messages, PATCH, or gateway MESSAGE_CREATE.
 */
interface APIMessage {
    /** Message ID. */
    id: Snowflake;
    /** Channel containing the message. */
    channel_id: Snowflake;
    /** Guild containing the message (if guild message). */
    guild_id?: Snowflake | null;
    /** Author of the message. */
    author: APIUser;
    /** Webhook ID if message was sent by a webhook. */
    webhook_id?: Snowflake | null;
    /** Message type (see {@link MessageType}). */
    type: MessageType;
    /** MessageFlags bitfield (see {@link MessageFlags}). */
    flags: number;
    /** Message text content. */
    content: string;
    /** ISO-8601 timestamp when the message was sent. */
    timestamp: string;
    /** ISO-8601 timestamp when the message was last edited (null if never edited). */
    edited_timestamp?: string | null;
    /** Whether the message is pinned. */
    pinned: boolean;
    /** Whether the message mentions @everyone or @here. */
    mention_everyone: boolean;
    /** Whether the message is text-to-speech. */
    tts: boolean;
    /** Users mentioned in the message. */
    mentions: APIUser[];
    /** Role IDs mentioned in the message. */
    mention_roles: Snowflake[];
    /** Channels mentioned that are visible to @everyone. */
    mention_channels?: Array<{
        id: Snowflake;
        guild_id: Snowflake;
        name: string;
        type: number;
    }> | null;
    /** Users referenced from non-notifying content (client resolution). */
    users?: APIUser[] | null;
    /** Embeds attached to the message. */
    embeds?: APIEmbed[] | null;
    /** File attachments. */
    attachments?: APIMessageAttachment[] | null;
    /** Stickers in the message. */
    stickers?: APIMessageSticker[] | null;
    /** IDs of custom NSFW emojis in this message. */
    nsfw_emojis?: Snowflake[];
    /** Reactions to the message. */
    reactions?: APIMessageReaction[] | null;
    /** Reply/forward reference (see {@link APIMessageReference}). */
    message_reference?: APIMessageReference | null;
    /** Snapshots of forwarded messages. */
    message_snapshots?: APIMessageSnapshot[] | null;
    /** Client-generated unique ID for deduplication. */
    nonce?: string | null;
    /** Call info when type is {@link MessageType.Call}. */
    call?: APIMessageCall | null;
    /** Referenced message (for replies, when available). */
    referenced_message?: APIMessage | null;
    /** Author's guild member object (gateway MESSAGE_CREATE only, when guild message). */
    member?: APIGuildMember | null;
}
/** Single channel window in POST /channels/messages/bulk request. */
interface APIBulkMessageFetchRequestItem {
    /** Channel to fetch messages from. */
    channel_id: Snowflake;
    /** Maximum number of messages to fetch. */
    limit: number;
    /** Fetch messages before this ID. */
    before?: Snowflake;
    /** Fetch messages after this ID. */
    after?: Snowflake;
    /** Fetch messages around this ID. */
    around?: Snowflake;
}
/** Request body for POST /channels/messages/bulk. */
interface APIBulkMessageFetchRequest {
    /** List of channel windows to fetch. */
    requests: APIBulkMessageFetchRequestItem[];
}
/** Per-channel messages in bulk fetch response. */
interface APIBulkMessageFetchResponseChannel {
    /** Channel ID. */
    channel_id: Snowflake;
    /** Messages from this channel. */
    messages: APIMessage[];
}
/** Response from POST /channels/messages/bulk. */
interface APIBulkMessageFetchResponse {
    /** List of per-channel message arrays. */
    channels: APIBulkMessageFetchResponseChannel[];
}

/** Bot/application info from GET /applications/@me. */
interface APIApplicationMe {
    /** Application ID. */
    id: Snowflake;
    /** Application name. */
    name: string;
    /** Application icon hash. */
    icon?: string | null;
    /** Application description. */
    description?: string | null;
    /** Whether the bot is public (can be added by anyone). */
    bot_public?: boolean;
    /** Whether the bot requires OAuth2 code grant flow. */
    bot_require_code_grant?: boolean;
    /** Verification key for interactions. */
    verify_key?: string;
    /** Application owner. */
    owner?: APIUserPartial | null;
    /** Application flags bitfield. */
    flags?: number;
}
/** Application from GET /oauth2/applications/@me. */
interface APIOAuthApplication {
    /** Application ID. */
    id: Snowflake;
    /** Application name. */
    name: string;
    /** OAuth2 redirect URIs. */
    redirect_uris?: string[];
    /** Whether the bot is public. */
    bot_public?: boolean;
    /** Whether the bot requires OAuth2 code grant flow. */
    bot_require_code_grant?: boolean;
    /** OAuth2 client secret. */
    client_secret?: string;
    /** Associated bot user. */
    bot?: APIUserPartial | null;
    /** Application icon hash. */
    icon?: string | null;
    /** Application description. */
    description?: string | null;
}
/** Response from GET /users/check-tag. */
interface APIUserTagCheck {
    /** Whether the username#discriminator is taken. */
    taken: boolean;
}
/** Response from POST /users/@me/preload-messages — map of channel ID → latest message or null. */
type APIPreloadMessagesResponse = Record<string, APIMessage | null>;
/** Request body for POST /users/@me/preload-messages. */
interface RESTPostAPIPreloadMessagesJSONBody {
    /** Channel IDs to preload latest messages for. */
    channels: Snowflake[];
}

/**
 * Presigned message attachment upload types (POST /channels/{id}/attachments).
 * Request ≠ response: plan uploads here, then reference `upload_filename` when sending.
 */
/** Single attachment to upload in request. */
interface RESTPostAPIChannelAttachmentUploadItem {
    /** Index-based ID for this attachment. */
    id: number;
    /** Original filename. */
    filename: string;
    /** File size in bytes. */
    file_size: number;
    /** MIME type. */
    content_type: string;
}
/** Request body for POST /channels/{id}/attachments (plan upload). */
interface RESTPostAPIChannelAttachmentUploadRequest {
    /** List of attachments to upload. */
    attachments: RESTPostAPIChannelAttachmentUploadItem[];
}
/** Singlepart upload response item (small files). */
interface RESTPostAPIChannelAttachmentUploadSinglepart {
    /** Index-based ID. */
    id: number;
    /** Original filename. */
    filename: string;
    /** CDN filename to reference when sending. */
    upload_filename: string;
    /** File size in bytes. */
    file_size: number;
    /** MIME type. */
    content_type: string;
    /** Upload mode. */
    upload_mode: 'singlepart';
    /** Presigned URL to PUT the file. */
    upload_url: string;
}
/** Single part in multipart upload. */
interface RESTPostAPIChannelAttachmentUploadMultipartPart {
    /** Part number (1-indexed). */
    part_number: number;
    /** Presigned URL to PUT this part. */
    upload_url: string;
}
/** Multipart upload response item (large files). */
interface RESTPostAPIChannelAttachmentUploadMultipart {
    /** Index-based ID. */
    id: number;
    /** Original filename. */
    filename: string;
    /** CDN filename to reference when sending. */
    upload_filename: string;
    /** File size in bytes. */
    file_size: number;
    /** MIME type. */
    content_type: string;
    /** Upload mode. */
    upload_mode: 'multipart';
    /** Multipart upload ID. */
    upload_id: string;
    /** Size of each part in bytes. */
    part_size: number;
    /** List of parts to upload. */
    parts: RESTPostAPIChannelAttachmentUploadMultipartPart[];
}
/** Union of singlepart and multipart upload response items. */
type RESTPostAPIChannelAttachmentUploadResponseItem = RESTPostAPIChannelAttachmentUploadSinglepart | RESTPostAPIChannelAttachmentUploadMultipart;
/** Response from POST /channels/{id}/attachments. */
interface RESTPostAPIChannelAttachmentUploadResponse {
    /** Upload instructions for each attachment. */
    attachments: RESTPostAPIChannelAttachmentUploadResponseItem[];
}
/** Single completed multipart upload. */
interface RESTPostAPIChannelAttachmentCompleteItem {
    /** CDN filename from upload response. */
    upload_filename: string;
    /** Multipart upload ID from upload response. */
    upload_id: string;
}
/** Request body for POST /channels/{id}/attachments/complete (finalize multipart uploads). */
interface RESTPostAPIChannelAttachmentCompleteRequest {
    /** List of completed uploads. */
    uploads: RESTPostAPIChannelAttachmentCompleteItem[];
}
/** Single completed upload confirmation. */
interface RESTPostAPIChannelAttachmentCompleteResult {
    /** CDN filename. */
    upload_filename: string;
}
/** Response from POST /channels/{id}/attachments/complete. */
interface RESTPostAPIChannelAttachmentCompleteResponse {
    /** Confirmed uploads. */
    uploads: RESTPostAPIChannelAttachmentCompleteResult[];
}
/** Multipart file attachment metadata (index-based id) for FormData sends. */
interface RESTPostAPIMessageAttachmentMeta {
    /** Index-based ID. */
    id: number;
    /** Filename. */
    filename: string;
    /** Display title. */
    title?: string | null;
    /** Alt text / caption. */
    description?: string | null;
    /** MessageAttachmentFlags bitfield. */
    flags?: number;
    /** MIME type. */
    content_type?: string;
    /** Duration in seconds (audio/video). */
    duration?: number | null;
    /** Base64 waveform (voice messages). */
    waveform?: string | null;
}
/**
 * Presigned-upload attachment reference for JSON message sends
 * (`ClientUploadedAttachmentRequest` in Fluxer schema).
 */
interface RESTPostAPIMessageUploadedAttachment {
    /** Index-based ID. */
    id: number;
    /** Original filename. */
    filename: string;
    /** CDN filename from presigned upload. */
    upload_filename: string;
    /** File size in bytes. */
    file_size: number;
    /** MIME type. */
    content_type: string;
    /** Display title. */
    title?: string | null;
    /** Alt text / caption. */
    description?: string | null;
    /** MessageAttachmentFlags bitfield. */
    flags?: number;
    /** Duration in seconds (audio/video). */
    duration?: number | null;
    /** Base64 waveform (voice messages). */
    waveform?: string | null;
}
/** Union of FormData and presigned attachment references. */
type RESTPostAPIMessageAttachment = RESTPostAPIMessageAttachmentMeta | RESTPostAPIMessageUploadedAttachment;
/** Type guard to check if attachment is presigned-uploaded. */
declare function isUploadedAttachment(a: RESTPostAPIMessageAttachment): a is RESTPostAPIMessageUploadedAttachment;

/** Guild ban from GET /guilds/{id}/bans. */
interface APIBan {
    /** Banned user. */
    user: APIUser;
    /** Ban reason (null if no reason provided). */
    reason?: string | null;
    /** Moderator who issued the ban (REST ban list). */
    moderator_id?: string;
    /** ISO-8601 timestamp when the ban was issued (REST ban list). */
    banned_at?: string;
    /** ISO-8601 timestamp when a temporary ban expires (null for permanent bans). */
    expires_at?: string | null;
}

/** Custom sticker reference. */
interface APISticker {
    /** Sticker ID. */
    id: Snowflake;
    /** Sticker name. */
    name: string;
    /** Sticker description. */
    description: string;
    /** Sticker tags for search. */
    tags: string[];
    /** Whether the sticker is animated. */
    animated: boolean;
    /** Whether the sticker is NSFW. */
    nsfw: boolean;
}
/** Custom sticker with user context (who created it). */
interface APIStickerWithUser extends APISticker {
    /** User who created the sticker. */
    user?: APIUser;
}
/** Response from GET /stickers/{sticker_id}/metadata. */
interface APIStickerMetadata {
    /** Sticker ID. */
    id: Snowflake;
    /** Guild containing the sticker. */
    guild_id: Snowflake;
    /** Sticker name. */
    name: string;
    /** Whether the sticker is animated. */
    animated: boolean;
    /** Whether the sticker can be cloned to other guilds. */
    allow_cloning: boolean;
}
/** Request body for POST /guilds/{id}/stickers (create sticker). */
interface RESTPostAPIGuildStickerJSONBody {
    /** Sticker name. */
    name: string;
    /** Sticker image data (base64). */
    image: string;
    /** Sticker description. */
    description?: string | null;
    /** Sticker tags. */
    tags?: string[];
}
/** Request body for PATCH /guilds/{id}/stickers/{sticker_id} (update sticker). */
interface RESTPatchAPIGuildStickerJSONBody {
    /** New sticker name. */
    name?: string;
    /** New sticker description. */
    description?: string | null;
    /** New sticker tags. */
    tags?: string[];
}
/** Response from POST /guilds/{id}/stickers/bulk (bulk create stickers). */
interface APIGuildStickerBulkCreateResponse {
    /** Successfully created stickers. */
    success: APISticker[];
    /** Failed sticker creations with error messages. */
    failed: Array<{
        name: string;
        error: string;
    }>;
}

/** Custom emoji reference. */
interface APIEmoji {
    /** Emoji ID. */
    id: Snowflake;
    /** Emoji name. */
    name: string;
    /** Whether the emoji is animated. */
    animated: boolean;
    /** Whether the emoji is NSFW. */
    nsfw: boolean;
}
/** Custom emoji with user context (who created it). */
interface APIEmojiWithUser extends APIEmoji {
    /** User who created the emoji. */
    user?: APIUser;
}
/** Response from GET /emojis/{emoji_id}/metadata. */
interface APIEmojiMetadata {
    /** Emoji ID. */
    id: Snowflake;
    /** Guild containing the emoji. */
    guild_id: Snowflake;
    /** Emoji name. */
    name: string;
    /** Whether the emoji is animated. */
    animated: boolean;
    /** Whether the emoji can be cloned to other guilds. */
    allow_cloning: boolean;
}
/** Request body for POST /guilds/{id}/emojis (create emoji). */
interface RESTPostAPIGuildEmojiJSONBody {
    /** Emoji name. */
    name: string;
    /** Emoji image data (base64). */
    image: string;
}
/** Request body for PATCH /guilds/{id}/emojis/{emoji_id} (update emoji). */
interface RESTPatchAPIGuildEmojiJSONBody {
    /** New emoji name. */
    name: string;
}
/** Response from POST /guilds/{id}/emojis/bulk (bulk create emojis). */
interface APIGuildEmojiBulkCreateResponse {
    /** Successfully created emojis. */
    success: APIEmoji[];
    /** Failed emoji creations with error messages. */
    failed: Array<{
        name: string;
        error: string;
    }>;
}

/**
 * Role from GET /guilds/{id}/roles.
 * `permissions` is a bitfield as string (e.g. "8933636165185").
 */
interface APIRole {
    /** Role ID. */
    id: Snowflake;
    /** Role name. */
    name: string;
    /** Role color as RGB integer. */
    color: number;
    /** Sort position in the role list. */
    position: number;
    /** Hoisted position (displayed separately in member list). */
    hoist_position?: number | null;
    /** Permissions bitfield as string. */
    permissions: string;
    /** Whether the role is hoisted (displayed separately). */
    hoist: boolean;
    /** Whether the role can be mentioned. */
    mentionable: boolean;
    /** Unicode emoji for the role (if no custom icon). */
    unicode_emoji?: string | null;
}
/** Request body for POST /guilds/{id}/roles (create role). All fields optional. */
interface RESTCreateRoleBody {
    /** Role name. */
    name?: string;
    /** Permissions bitfield as string. */
    permissions?: string;
    /** Role color as RGB integer. */
    color?: number;
    /** Whether the role is hoisted. */
    hoist?: boolean;
    /** Whether the role can be mentioned. */
    mentionable?: boolean;
    /** Unicode emoji for the role. */
    unicode_emoji?: string | null;
    /** Sort position. */
    position?: number;
    /** Hoisted position. */
    hoist_position?: number | null;
}
/** Request body for PATCH /guilds/{id}/roles/{roleId} (update role). All fields optional. */
interface RESTUpdateRoleBody {
    /** Role name. */
    name?: string;
    /** Permissions bitfield as string. */
    permissions?: string;
    /** Role color as RGB integer. */
    color?: number;
    /** Whether the role is hoisted. */
    hoist?: boolean;
    /** Whether the role can be mentioned. */
    mentionable?: boolean;
    /** Unicode emoji for the role. */
    unicode_emoji?: string | null;
    /** Sort position. */
    position?: number;
    /** Hoisted position. */
    hoist_position?: number | null;
}

/**
 * Webhook type (OpenAPI WebhookType).
 * - `Incoming` — standard webhook created by user/bot
 * - `ChannelFollower` — webhook created by following an announcement channel
 */
declare enum WebhookType {
    Incoming = 1,
    ChannelFollower = 2
}
/**
 * Webhook from GET /channels/{id}/webhooks (includes token) or GET /webhooks/{id} (no token).
 */
interface APIWebhook {
    /** Webhook ID. */
    id: Snowflake;
    /** Guild containing the webhook. */
    guild_id: Snowflake;
    /** Channel the webhook posts to. */
    channel_id: Snowflake;
    /** Webhook display name. */
    name: string;
    /** Webhook avatar hash. */
    avatar?: string | null;
    /** Webhook token (present when listing channel webhooks). */
    token: string;
    /** User who created the webhook. */
    user: APIUser;
    /** Webhook type (audit-log / some list responses). */
    type?: WebhookType;
}
/** Request body for PATCH /webhooks/{id} (bot auth). All fields optional. */
interface APIWebhookUpdateRequest {
    /** New webhook name. */
    name?: string;
    /** New webhook avatar (base64 or null to remove). */
    avatar?: string | null;
    /** Move webhook to a different channel. */
    channel_id?: Snowflake;
}
/** Request body for PATCH /webhooks/{id}/{token} (token auth). All fields optional. */
interface APIWebhookTokenUpdateRequest {
    /** New webhook name. */
    name?: string;
    /** New webhook avatar (base64 or null to remove). */
    avatar?: string | null;
}
/** Request body for PATCH /webhooks/{id}/{token}/messages/{message_id}. All fields optional. */
interface APIWebhookEditMessageRequest {
    /** New message content. */
    content?: string;
    /** New embeds. */
    embeds?: APIEmbed[];
    /** Attachment updates. */
    attachments?: unknown[];
}

/**
 * Guild verification level (OpenAPI VerificationLevel).
 * - `None` — unrestricted
 * - `Low` — must have verified email
 * - `Medium` — must be registered for 5+ minutes
 * - `High` — must be member for 10+ minutes
 * - `VeryHigh` — must have verified phone
 */
declare enum GuildVerificationLevel {
    None = 0,
    Low = 1,
    Medium = 2,
    High = 3,
    VeryHigh = 4
}
/**
 * Guild MFA level (OpenAPI MFALevel).
 * - `None` — no MFA requirement
 * - `Elevated` — moderators must have MFA enabled
 */
declare enum GuildMFALevel {
    None = 0,
    Elevated = 1
}
/**
 * Guild explicit content filter (OpenAPI ExplicitContentFilterLevel).
 * - `Disabled` — no content scanning
 * - `MembersWithoutRoles` — scan messages from members without roles
 * - `AllMembers` — scan all messages
 */
declare enum GuildExplicitContentFilter {
    Disabled = 0,
    MembersWithoutRoles = 1,
    AllMembers = 2
}
/**
 * Default message notification level (OpenAPI DefaultMessageNotificationLevel).
 * - `AllMessages` — notify for all messages
 * - `OnlyMentions` — notify only for mentions
 */
declare enum DefaultMessageNotifications {
    AllMessages = 0,
    OnlyMentions = 1
}
/**
 * Guild NSFW level (OpenAPI NSFWLevel).
 * - `Safe` — guild is safe for work
 * - `AgeRestricted` — guild is age-restricted / NSFW
 */
declare enum GuildNSFWLevel {
    Safe = 0,
    AgeRestricted = 3
}
/**
 * Content warning level for guild / category / channel (OpenAPI ContentWarningLevel).
 * - `Inherit` — inherit from parent
 * - `ContentWarning` — show content warning
 */
declare enum ContentWarningLevel {
    Inherit = 0,
    ContentWarning = 1
}
/**
 * Splash card alignment (OpenAPI GuildUpdateRequest.splash_card_alignment).
 * - `Center` — center alignment
 * - `Left` — left alignment
 * - `Right` — right alignment
 */
declare enum SplashCardAlignment {
    Center = 0,
    Left = 1,
    Right = 2
}
/**
 * Audit log action type (OpenAPI AuditLogActionType).
 * Each value represents a specific moderation or administrative action.
 */
declare enum AuditLogActionType {
    /** Guild settings updated */
    GuildUpdate = 1,
    /** Channel created */
    ChannelCreate = 10,
    /** Channel settings updated */
    ChannelUpdate = 11,
    /** Channel deleted */
    ChannelDelete = 12,
    /** Channel permission overwrite created */
    ChannelOverwriteCreate = 13,
    /** Channel permission overwrite updated */
    ChannelOverwriteUpdate = 14,
    /** Channel permission overwrite deleted */
    ChannelOverwriteDelete = 15,
    /** Member kicked from guild */
    MemberKick = 20,
    /** Members pruned (inactive cleanup) */
    MemberPrune = 21,
    /** Member banned */
    MemberBanAdd = 22,
    /** Member unbanned */
    MemberBanRemove = 23,
    /** Member profile updated */
    MemberUpdate = 24,
    /** Member roles changed */
    MemberRoleUpdate = 25,
    /** Members moved to different voice channel */
    MemberMove = 26,
    /** Members disconnected from voice */
    MemberDisconnect = 27,
    /** Bot added to guild */
    BotAdd = 28,
    /** Role created */
    RoleCreate = 30,
    /** Role settings updated */
    RoleUpdate = 31,
    /** Role deleted */
    RoleDelete = 32,
    /** Invite created */
    InviteCreate = 40,
    /** Invite settings updated */
    InviteUpdate = 41,
    /** Invite deleted */
    InviteDelete = 42,
    /** Webhook created */
    WebhookCreate = 50,
    /** Webhook settings updated */
    WebhookUpdate = 51,
    /** Webhook deleted */
    WebhookDelete = 52,
    /** Custom emoji created */
    EmojiCreate = 60,
    /** Custom emoji updated */
    EmojiUpdate = 61,
    /** Custom emoji deleted */
    EmojiDelete = 62,
    /** Message deleted */
    MessageDelete = 72,
    /** Multiple messages deleted at once */
    MessageBulkDelete = 73,
    /** Message pinned */
    MessagePin = 74,
    /** Message unpinned */
    MessageUnpin = 75,
    /** Custom sticker created */
    StickerCreate = 90,
    /** Custom sticker updated */
    StickerUpdate = 91,
    /** Custom sticker deleted */
    StickerDelete = 92
}
/**
 * System channel message flags (OpenAPI SystemChannelFlags).
 * - `SuppressJoinNotifications` — disable member join messages
 * Bitfield — compose with bitwise OR.
 */
declare const SystemChannelFlags: {
    readonly SuppressJoinNotifications: 1;
};
/** Union of all valid {@link SystemChannelFlags} values. */
type SystemChannelFlagsValue = (typeof SystemChannelFlags)[keyof typeof SystemChannelFlags];
/**
 * Disabled guild operations bitfield (OpenAPI GuildOperations).
 * - `PushNotifications` — push notifications disabled
 * - `EveryoneMentions` — @everyone/@here mentions disabled
 * - `TypingEvents` — typing indicators disabled
 * - `InstantInvites` — instant invites disabled
 * - `SendMessage` — sending messages disabled
 * - `Reactions` — reactions disabled
 * - `MemberListUpdates` — member list updates disabled
 * Bits set = operation disabled.
 */
declare const GuildOperations: {
    readonly PushNotifications: 1;
    readonly EveryoneMentions: 2;
    readonly TypingEvents: 4;
    readonly InstantInvites: 8;
    readonly SendMessage: 16;
    readonly Reactions: 32;
    readonly MemberListUpdates: 64;
};
/** Union of all valid {@link GuildOperations} values. */
type GuildOperationsValue = (typeof GuildOperations)[keyof typeof GuildOperations];
/**
 * Guild feature flags (OpenAPI GuildFeatures).
 * - `ANIMATED_ICON` — animated guild icon
 * - `ANIMATED_BANNER` — animated guild banner
 * - `BANNER` — guild banner
 * - `DETACHED_BANNER` — banner on separate canvas
 * - `INVITE_SPLASH` — invite splash screen
 * - `INVITES_DISABLED` — invites disabled
 * - `TEXT_CHANNEL_FLEXIBLE_NAMES` — flexible text channel naming
 * - `MORE_EMOJI` — extra emoji slots
 * - `MORE_STICKERS` — extra sticker slots
 * - `UNLIMITED_EMOJI` — unlimited emoji
 * - `UNLIMITED_STICKERS` — unlimited stickers
 * - `EXPRESSION_PURGE_ALLOWED` — can purge emoji/stickers
 * - `VANITY_URL` — custom vanity URL
 * - `VERIFIED` — verified guild badge
 * - `VIP_VOICE` — VIP voice regions
 * - `UNAVAILABLE_FOR_EVERYONE` — guild unavailable for non-staff
 * - `UNAVAILABLE_FOR_EVERYONE_BUT_STAFF` — guild unavailable except for staff
 * - `VISIONARY` — visionary guild badge
 * - `OPERATOR` — operator guild badge
 * - `LARGE_GUILD_OVERRIDE` — large guild override
 * - `VERY_LARGE_GUILD` — very large guild (>100k members)
 * - `MT_MESSAGE_SCHEDULING` — message scheduling enabled
 * - `MT_EXPRESSION_PACKS` — expression packs enabled
 */
type GuildFeature = 'ANIMATED_ICON' | 'ANIMATED_BANNER' | 'BANNER' | 'DETACHED_BANNER' | 'INVITE_SPLASH' | 'INVITES_DISABLED' | 'TEXT_CHANNEL_FLEXIBLE_NAMES' | 'MORE_EMOJI' | 'MORE_STICKERS' | 'UNLIMITED_EMOJI' | 'UNLIMITED_STICKERS' | 'EXPRESSION_PURGE_ALLOWED' | 'VANITY_URL' | 'VERIFIED' | 'VIP_VOICE' | 'UNAVAILABLE_FOR_EVERYONE' | 'UNAVAILABLE_FOR_EVERYONE_BUT_STAFF' | 'VISIONARY' | 'OPERATOR' | 'LARGE_GUILD_OVERRIDE' | 'VERY_LARGE_GUILD' | 'MT_MESSAGE_SCHEDULING' | 'MT_EXPRESSION_PACKS';
/**
 * Full guild object from GET /guilds/{id} or gateway GUILD_CREATE.
 */
interface APIGuild {
    /** Guild ID. */
    id: Snowflake;
    /** Guild name. */
    name: string;
    /** Guild icon hash. */
    icon?: string | null;
    /** Guild banner hash. */
    banner?: string | null;
    /** Banner width in pixels. */
    banner_width?: number | null;
    /** Banner height in pixels. */
    banner_height?: number | null;
    /** Invite splash image hash. */
    splash?: string | null;
    /** Splash width in pixels. */
    splash_width?: number | null;
    /** Splash height in pixels. */
    splash_height?: number | null;
    /** Splash card alignment (see {@link SplashCardAlignment}). */
    splash_card_alignment: SplashCardAlignment;
    /** Embed splash image hash (deprecated). */
    embed_splash?: string | null;
    /** Embed splash width in pixels. */
    embed_splash_width?: number | null;
    /** Embed splash height in pixels. */
    embed_splash_height?: number | null;
    /** Custom vanity URL code. */
    vanity_url_code?: string | null;
    /** Guild owner user ID. */
    owner_id: Snowflake;
    /** System channel ID (join/boost messages). */
    system_channel_id?: Snowflake | null;
    /** {@link SystemChannelFlags} bitfield. */
    system_channel_flags: number;
    /** Rules channel ID. */
    rules_channel_id?: Snowflake | null;
    /** AFK voice channel ID. */
    afk_channel_id?: Snowflake | null;
    /** AFK timeout in seconds. */
    afk_timeout: number;
    /** Guild feature flags (see {@link GuildFeature}). */
    features: GuildFeature[];
    /** Verification level (see {@link GuildVerificationLevel}). */
    verification_level: GuildVerificationLevel;
    /** MFA requirement level (see {@link GuildMFALevel}). */
    mfa_level: GuildMFALevel;
    /** NSFW level (see {@link GuildNSFWLevel}). */
    nsfw_level: GuildNSFWLevel;
    /** Whether the guild is marked as adult (18+) content. */
    nsfw: boolean;
    /** Explicit content filter level (see {@link GuildExplicitContentFilter}). */
    explicit_content_filter: GuildExplicitContentFilter;
    /** Default notification level (see {@link DefaultMessageNotifications}). */
    default_message_notifications: DefaultMessageNotifications;
    /** {@link GuildOperations} bitfield of disabled operations. */
    disabled_operations: number;
    /** Content warning level (see {@link ContentWarningLevel}). */
    content_warning_level: ContentWarningLevel;
    /** Custom guild-wide content warning text. */
    content_warning_text?: string | null;
    /** ISO-8601 timestamp before which message history is not available. */
    message_history_cutoff?: string | null;
    /** Permissions for the current user in this guild (bitfield string). */
    permissions?: string;
    /** Roles when included in a gateway/REST guild payload. */
    roles?: APIRole[];
    /** Emojis when included in a gateway/REST guild payload. */
    emojis?: APIEmoji[];
    /** Stickers when included in a gateway/REST guild payload. */
    stickers?: APISticker[];
    /** Channels when included in a gateway/REST guild payload. */
    channels?: APIChannel[];
    /** Member count when included (gateway / guild payloads). */
    member_count?: number;
    /** Online member count when included (gateway / guild payloads). */
    online_count?: number;
    /** Approximate member count when `with_counts` is set on guild list. */
    approximate_member_count?: number;
    /** Approximate online count when `with_counts` is set on guild list. */
    approximate_presence_count?: number;
}
/** Audit log entry from GET /guilds/{id}/audit-logs. */
interface APIGuildAuditLogEntry {
    /** Entry ID. */
    id: string;
    /** Action type (see {@link AuditLogActionType}). */
    action_type: AuditLogActionType;
    /** User who performed the action. */
    user_id?: Snowflake | null;
    /** Target entity ID (channel, member, role, etc.). */
    target_id?: Snowflake | null;
    /** Reason provided for the action. */
    reason?: string;
    /** Action-specific metadata (e.g. role name, channel name). */
    options?: Record<string, string>;
    /** Changed fields; value types vary by action_type (e.g. string for name, number for permissions). */
    changes?: Array<{
        key: string;
        old_value?: string | number | boolean | null;
        new_value?: string | number | boolean | null;
    }>;
}
/** Response from GET /guilds/{id}/vanity-url. */
interface APIVanityURL {
    /** Vanity URL code (null if not set). */
    code: string | null;
    /** Number of times the vanity URL has been used. */
    uses: number;
}
/** Request body for guild feature toggles (text-channel-flexible-names, etc.). */
interface APIGuildFeatureToggle {
    /** Whether to enable the feature. */
    enabled: boolean;
}
/** Response from GET /guilds/{id}/audit-logs. */
interface APIGuildAuditLog {
    /** Audit log entries. */
    audit_log_entries: APIGuildAuditLogEntry[];
    /** Partial user objects referenced in the log. */
    users: Array<{
        id: Snowflake;
        username?: string;
        discriminator?: string;
        avatar?: string | null;
    }>;
    /** Partial webhook objects referenced in the log. */
    webhooks: Array<{
        id: Snowflake;
        name?: string;
        avatar?: string | null;
        type?: WebhookType;
    }>;
}
/** Request body for POST /guilds/{id}/members-search. */
interface APIGuildMemberSearchRequest {
    /** Username or nickname search query. */
    query?: string;
    /** Maximum results to return. */
    limit?: number;
    /** Result offset for pagination. */
    offset?: number;
    /** Filter by role IDs. */
    role_ids?: Snowflake[];
    /** Filter by joined date (Unix timestamp, greater or equal). */
    joined_at_gte?: number;
    /** Filter by joined date (Unix timestamp, less or equal). */
    joined_at_lte?: number;
    /** Filter by bot status. */
    is_bot?: boolean;
    /** Filter by user creation date (Unix timestamp, greater or equal). */
    user_created_at_gte?: number;
    /** Filter by user creation date (Unix timestamp, less or equal). */
    user_created_at_lte?: number;
    /** Sort field. */
    sort_by?: 'joinedAt' | 'relevance';
    /** Sort order. */
    sort_order?: 'asc' | 'desc';
}
/** Single result from guild member search. */
interface APIGuildMemberSearchResult {
    /** Member ID. */
    id: string;
    /** Guild ID. */
    guild_id: Snowflake;
    /** User ID. */
    user_id: Snowflake;
    /** Username. */
    username: string;
    /** Discriminator. */
    discriminator: string;
    /** Display name. */
    global_name: string | null;
    /** Guild nickname. */
    nickname: string | null;
}
/** Response from POST /guilds/{id}/members-search. */
interface APIGuildMemberSearchResponse {
    /** Guild ID. */
    guild_id: Snowflake;
    /** Search results for this page. */
    members: APIGuildMemberSearchResult[];
    /** Number of results on this page. */
    page_result_count: number;
    /** Total results across all pages. */
    total_result_count: number;
    /** Whether the search index is still building. */
    indexing?: boolean;
}

/**
 * Channel type enum (Fluxer OpenAPI).
 * - `GuildText` — text channel in a guild
 * - `DM` — private DM between two users
 * - `GuildVoice` — voice channel in a guild
 * - `GroupDM` — group DM with multiple users
 * - `GuildCategory` — category container for channels
 * - `GuildLink` — Fluxer link channel (OpenAPI GUILD_LINK, type 998)
 * - `DMPersonalNotes` — Fluxer personal notes DM (OpenAPI DM_PERSONAL_NOTES, type 999)
 */
declare enum ChannelType {
    GuildText = 0,
    DM = 1,
    GuildVoice = 2,
    GroupDM = 3,
    GuildCategory = 4,
    GuildLink = 998,
    DMPersonalNotes = 999
}
/**
 * Permission overwrite type.
 * - `Role` — overwrite applies to a role
 * - `Member` — overwrite applies to a specific member
 */
declare enum OverwriteType {
    Role = 0,
    Member = 1
}
/** Permission overwrite from GET /channels/{id} or GET /guilds/{id}/channels. */
interface APIChannelOverwrite {
    /** Role or member ID. */
    id: Snowflake;
    /** Whether this is a role or member overwrite. */
    type: OverwriteType;
    /** Permissions allowed (as bitfield string). */
    allow: string;
    /** Permissions denied (as bitfield string). */
    deny: string;
}
/** Minimal channel (id and type required). */
interface APIChannelPartial {
    /** Channel ID. */
    id: Snowflake;
    /** Channel name. */
    name?: string | null;
    /** Channel type (see {@link ChannelType}). */
    type: ChannelType;
    /** Channel icon hash (group DMs / some partials). */
    icon?: string | null;
    /** Parent category ID (some partials). */
    parent_id?: Snowflake | null;
    /** DM recipients (username only on some partial payloads). */
    recipients?: Array<APIUser | {
        username: string;
    }>;
}
/**
 * Full channel object from GET /channels/{id} or GET /guilds/{id}/channels.
 */
interface APIChannel extends Omit<APIChannelPartial, 'name' | 'recipients'> {
    /** Guild ID (absent for DMs). */
    guild_id?: Snowflake;
    /** Channel name. */
    name?: string;
    /** Channel topic. */
    topic?: string | null;
    /** External URL (link channels, type 998). */
    url?: string | null;
    /** Channel icon hash. */
    icon?: string | null;
    /** Owner ID (group DMs). */
    owner_id?: Snowflake | null;
    /** Sort position in the channel list. */
    position?: number;
    /** Parent category ID. */
    parent_id?: Snowflake | null;
    /** Voice bitrate (kbps). */
    bitrate?: number | null;
    /** Voice user limit (0 = unlimited). */
    user_limit?: number | null;
    /** Max active voice connections per user in this channel. */
    voice_connection_limit?: number | null;
    /** Voice RTC region ID. */
    rtc_region?: string | null;
    /** Last message ID in this channel. */
    last_message_id?: Snowflake | null;
    /** ISO-8601 timestamp of last pin. */
    last_pin_timestamp?: string | null;
    /** Permission overwrites. */
    permission_overwrites?: APIChannelOverwrite[];
    /** DM/group DM recipients. */
    recipients?: APIUser[];
    /** Whether the channel is NSFW. */
    nsfw?: boolean;
    /** Per-channel NSFW override (`null` inherits). */
    nsfw_override?: boolean | null;
    /** Slowmode delay in seconds. */
    rate_limit_per_user?: number;
    /** User-specific nicknames in group DMs (user_id -> nick). */
    nicks?: Record<string, string>;
    /** Content warning level. */
    content_warning_level?: ContentWarningLevel;
    /** Content warning custom text. */
    content_warning_text?: string | null;
}
/** RTC region from GET /channels/{id}/rtc-regions. */
interface APIRtcRegion {
    /** Region ID. */
    id: string;
    /** Human-readable region name. */
    name: string;
    /** Region flag emoji. */
    emoji: string;
}
/** Slowmode state from GET /channels/{id}/slowmode. */
interface APIChannelSlowmodeState {
    /** Slowmode delay in seconds. */
    rate_limit_per_user: number;
    /** Milliseconds until the user can send again. */
    retry_after_ms: number;
    /** ISO-8601 timestamp when the user can send again. */
    next_send_allowed_at: string | null;
}
/** Single pin from GET /channels/{id}/messages/pins. */
interface APIChannelPinItem {
    /** The pinned message. */
    message: APIMessage;
    /** ISO-8601 timestamp when pinned. */
    pinned_at: string;
}
/** Paginated pins response from GET /channels/{id}/messages/pins. */
interface APIChannelPinsPage {
    /** Pinned messages on this page. */
    items: APIChannelPinItem[];
    /** Whether more pins exist. */
    has_more: boolean;
}
/** Shared optional fields on guild channel create bodies. */
interface ChannelCreateShared {
    /** Channel topic. */
    topic?: string | null;
    /** External URL (link channels). */
    url?: string | null;
    /** Parent category ID. */
    parent_id?: Snowflake | null;
    /** Voice bitrate (kbps). */
    bitrate?: number | null;
    /** Voice user limit. */
    user_limit?: number | null;
    /** Voice connection limit. */
    voice_connection_limit?: number | null;
    /** Permission overwrites. */
    permission_overwrites?: APIChannelOverwrite[];
    /** Whether the channel is NSFW. */
    nsfw?: boolean;
    /** NSFW override flag. */
    nsfw_override?: boolean | null;
    /** Content warning level. */
    content_warning_level?: ContentWarningLevel;
    /** Content warning custom text. */
    content_warning_text?: string | null;
    /** Slowmode delay in seconds (0–21600). */
    rate_limit_per_user?: number | null;
    /** Channel name. */
    name: string;
}
/** POST /guilds/{id}/channels — text channel (type 0). */
interface GuildTextChannelCreateRequest extends ChannelCreateShared {
    type: ChannelType.GuildText;
}
/** POST /guilds/{id}/channels — voice channel (type 2). */
interface GuildVoiceChannelCreateRequest extends ChannelCreateShared {
    type: ChannelType.GuildVoice;
}
/** POST /guilds/{id}/channels — category (type 4). */
interface GuildCategoryChannelCreateRequest extends ChannelCreateShared {
    type: ChannelType.GuildCategory;
}
/** POST /guilds/{id}/channels — link channel (type 998); set `url`. */
interface GuildLinkChannelCreateRequest extends ChannelCreateShared {
    type: ChannelType.GuildLink;
    url?: string | null;
}
/** Union of all valid channel create request types. */
type ChannelCreateRequest = GuildTextChannelCreateRequest | GuildVoiceChannelCreateRequest | GuildCategoryChannelCreateRequest | GuildLinkChannelCreateRequest;

/** Response from GET /guilds/{id}/discovery (discovery eligibility and current application). */
interface APIDiscoveryStatus {
    /** Current discovery application (if applied). */
    application?: APIDiscoveryApplication | null;
    /** Whether the guild is eligible for discovery. */
    eligible: boolean;
    /** Minimum member count required for discovery. */
    min_member_count: number;
}
/** Discovery application payload (GET status / POST apply / PATCH edit). */
interface APIDiscoveryApplication {
    /** Guild ID. */
    guild_id: Snowflake;
    /** Guild NSFW level. */
    guild_nsfw_level?: GuildNSFWLevel | null;
    /** Application status (pending, approved, rejected, etc.). */
    status: string;
    /** Guild description for discovery. */
    description: string;
    /** Discovery category ID from the discovery categories list. */
    category_type: number;
    /** Primary language code. */
    primary_language?: string | null;
    /** Custom tags for search. */
    custom_tags?: string[];
    /** ISO-8601 timestamp when applied. */
    applied_at?: string;
    /** ISO-8601 timestamp when reviewed. */
    reviewed_at?: string | null;
}
/** Request body for POST /guilds/{id}/discovery (apply for discovery). */
interface RESTPostAPIGuildDiscoveryJSONBody {
    /** Guild description. */
    description: string;
    /** Discovery category ID. */
    category_type: number;
    /** Primary language code. */
    primary_language?: string;
    /** Custom tags. */
    custom_tags?: string[];
}
/** Request body for PATCH /guilds/{id}/discovery (update discovery application). */
interface RESTPatchAPIGuildDiscoveryJSONBody {
    /** Guild description. */
    description?: string;
    /** Discovery category ID. */
    category_type?: number;
    /** Primary language code. */
    primary_language?: string;
    /** Custom tags. */
    custom_tags?: string[];
}

/**
 * API error codes returned by the Fluxer API.
 * Aligned with fluxer `packages/constants/src/ApiErrorCodes.ts`.
 */
declare enum APIErrorCode {
    AccessDenied = "ACCESS_DENIED",
    AccountDisabled = "ACCOUNT_DISABLED",
    BadGateway = "BAD_GATEWAY",
    BadRequest = "BAD_REQUEST",
    BlueskyOauthAuthorizationFailed = "BLUESKY_OAUTH_AUTHORIZATION_FAILED",
    BlueskyOauthCallbackFailed = "BLUESKY_OAUTH_CALLBACK_FAILED",
    BlueskyOauthNotEnabled = "BLUESKY_OAUTH_NOT_ENABLED",
    BlueskyOauthSessionExpired = "BLUESKY_OAUTH_SESSION_EXPIRED",
    BlueskyOauthStateInvalid = "BLUESKY_OAUTH_STATE_INVALID",
    AccountScheduledForDeletion = "ACCOUNT_SCHEDULED_FOR_DELETION",
    AccountSuspendedPermanently = "ACCOUNT_SUSPENDED_PERMANENTLY",
    AccountSuspendedTemporarily = "ACCOUNT_SUSPENDED_TEMPORARILY",
    AccountSuspiciousActivity = "ACCOUNT_SUSPICIOUS_ACTIVITY",
    AccountTooNewForGuild = "ACCOUNT_TOO_NEW_FOR_GUILD",
    AclsMustBeNonEmpty = "ACLS_MUST_BE_NON_EMPTY",
    AdminApiKeyNotFound = "ADMIN_API_KEY_NOT_FOUND",
    ApplicationNotFound = "APPLICATION_NOT_FOUND",
    ApplicationNotOwned = "APPLICATION_NOT_OWNED",
    AlreadyFriends = "ALREADY_FRIENDS",
    AuditLogIndexing = "AUDIT_LOG_INDEXING",
    BotsCannotCreateGuilds = "BOTS_CANNOT_CREATE_GUILDS",
    BotsCannotSendFriendRequests = "BOTS_CANNOT_SEND_FRIEND_REQUESTS",
    BotAlreadyInGuild = "BOT_ALREADY_IN_GUILD",
    BotApplicationNotFound = "BOT_APPLICATION_NOT_FOUND",
    BotIsPrivate = "BOT_IS_PRIVATE",
    BotUserAuthEndpointAccessDenied = "BOT_USER_AUTH_ENDPOINT_ACCESS_DENIED",
    BotUserAuthSessionCreationDenied = "BOT_USER_AUTH_SESSION_CREATION_DENIED",
    BotUserGenerationFailed = "BOT_USER_GENERATION_FAILED",
    BotUserNotFound = "BOT_USER_NOT_FOUND",
    CallAlreadyExists = "CALL_ALREADY_EXISTS",
    CannotEditOtherUserMessage = "CANNOT_EDIT_OTHER_USER_MESSAGE",
    CannotExecuteOnDm = "CANNOT_EXECUTE_ON_DM",
    CannotBlockSystemUser = "CANNOT_BLOCK_SYSTEM_USER",
    CannotModifySystemWebhook = "CANNOT_MODIFY_SYSTEM_WEBHOOK",
    CannotModifyVoiceState = "CANNOT_MODIFY_VOICE_STATE",
    CannotRedeemPlutoniumWithVisionary = "CANNOT_REDEEM_PLUTONIUM_WITH_VISIONARY",
    CannotReportGuild = "CANNOT_REPORT_GUILD",
    CannotReportOwnGuild = "CANNOT_REPORT_OWN_GUILD",
    CannotReportOwnMessage = "CANNOT_REPORT_OWN_MESSAGE",
    CannotReportYourself = "CANNOT_REPORT_YOURSELF",
    CannotSendEmptyMessage = "CANNOT_SEND_EMPTY_MESSAGE",
    CannotSendFriendRequestToBlockedUser = "CANNOT_SEND_FRIEND_REQUEST_TO_BLOCKED_USER",
    CannotSendFriendRequestToSelf = "CANNOT_SEND_FRIEND_REQUEST_TO_SELF",
    CannotSendMessagesInNonTextChannel = "CANNOT_SEND_MESSAGES_IN_NON_TEXT_CHANNEL",
    CannotSendMessagesToUser = "CANNOT_SEND_MESSAGES_TO_USER",
    CannotTransferOwnershipToBot = "CANNOT_TRANSFER_OWNERSHIP_TO_BOT",
    CannotShrinkReservedSlots = "CANNOT_SHRINK_RESERVED_SLOTS",
    CaptchaRequired = "CAPTCHA_REQUIRED",
    ChannelIndexing = "CHANNEL_INDEXING",
    CommunicationDisabled = "COMMUNICATION_DISABLED",
    ConnectionAlreadyExists = "CONNECTION_ALREADY_EXISTS",
    ConnectionInitiationTokenInvalid = "CONNECTION_INITIATION_TOKEN_INVALID",
    ConnectionInvalidIdentifier = "CONNECTION_INVALID_IDENTIFIER",
    ConnectionInvalidType = "CONNECTION_INVALID_TYPE",
    ConnectionLimitReached = "CONNECTION_LIMIT_REACHED",
    ConnectionNotFound = "CONNECTION_NOT_FOUND",
    ConnectionVerificationFailed = "CONNECTION_VERIFICATION_FAILED",
    Conflict = "CONFLICT",
    ContentBlocked = "CONTENT_BLOCKED",
    CreationFailed = "CREATION_FAILED",
    DecryptionFailed = "DECRYPTION_FAILED",
    DeletionFailed = "DELETION_FAILED",
    DiscoveryAlreadyApplied = "DISCOVERY_ALREADY_APPLIED",
    DiscoveryApplicationAlreadyReviewed = "DISCOVERY_APPLICATION_ALREADY_REVIEWED",
    DiscoveryApplicationNotFound = "DISCOVERY_APPLICATION_NOT_FOUND",
    DiscoveryDescriptionRequired = "DISCOVERY_DESCRIPTION_REQUIRED",
    DiscoveryDisabled = "DISCOVERY_DISABLED",
    DiscoveryInsufficientMembers = "DISCOVERY_INSUFFICIENT_MEMBERS",
    DiscoveryInvalidCategory = "DISCOVERY_INVALID_CATEGORY",
    DiscoveryNotDiscoverable = "DISCOVERY_NOT_DISCOVERABLE",
    DiscriminatorRequired = "DISCRIMINATOR_REQUIRED",
    EmailServiceNotTestable = "EMAIL_SERVICE_NOT_TESTABLE",
    EmailVerificationRequired = "EMAIL_VERIFICATION_REQUIRED",
    CanaryTesterEmailVerificationRequired = "CANARY_TESTER_EMAIL_VERIFICATION_REQUIRED",
    DirectMessageEmailVerificationRequired = "DIRECT_MESSAGE_EMAIL_VERIFICATION_REQUIRED",
    FriendRequestEmailVerificationRequired = "FRIEND_REQUEST_EMAIL_VERIFICATION_REQUIRED",
    GuildCreationEmailVerificationRequired = "GUILD_CREATION_EMAIL_VERIFICATION_REQUIRED",
    GuildEmailVerificationRequired = "GUILD_EMAIL_VERIFICATION_REQUIRED",
    MfaEmailVerificationRequired = "MFA_EMAIL_VERIFICATION_REQUIRED",
    ProfileEmailVerificationRequired = "PROFILE_EMAIL_VERIFICATION_REQUIRED",
    PurchaseEmailVerificationRequired = "PURCHASE_EMAIL_VERIFICATION_REQUIRED",
    ReactionEmailVerificationRequired = "REACTION_EMAIL_VERIFICATION_REQUIRED",
    ReportEmailVerificationRequired = "REPORT_EMAIL_VERIFICATION_REQUIRED",
    RegistrationClosed = "REGISTRATION_CLOSED",
    RegistrationPendingApproval = "REGISTRATION_PENDING_APPROVAL",
    RegistrationRejected = "REGISTRATION_REJECTED",
    RegistrationUrlInvalid = "REGISTRATION_URL_INVALID",
    EmptyEncryptedBody = "EMPTY_ENCRYPTED_BODY",
    EncryptionFailed = "ENCRYPTION_FAILED",
    EuWithdrawalWaiverRequired = "EU_WITHDRAWAL_WAIVER_REQUIRED",
    ExplicitContentCannotBeSent = "EXPLICIT_CONTENT_CANNOT_BE_SENT",
    FeatureNotAvailableSelfHosted = "FEATURE_NOT_AVAILABLE_SELF_HOSTED",
    FeatureTemporarilyDisabled = "FEATURE_TEMPORARILY_DISABLED",
    FileSizeTooLarge = "FILE_SIZE_TOO_LARGE",
    Forbidden = "FORBIDDEN",
    FriendRequestBlocked = "FRIEND_REQUEST_BLOCKED",
    GatewayTimeout = "GATEWAY_TIMEOUT",
    GeneralError = "GENERAL_ERROR",
    Gone = "GONE",
    GiftCodeAlreadyRedeemed = "GIFT_CODE_ALREADY_REDEEMED",
    GuildPhoneVerificationRequired = "GUILD_PHONE_VERIFICATION_REQUIRED",
    GuildTemplateFetchFailed = "GUILD_TEMPLATE_FETCH_FAILED",
    GuildTemplateInvalid = "GUILD_TEMPLATE_INVALID",
    GuildVerificationRequired = "GUILD_VERIFICATION_REQUIRED",
    HandoffCodeExpired = "HANDOFF_CODE_EXPIRED",
    HarvestExpired = "HARVEST_EXPIRED",
    HarvestFailed = "HARVEST_FAILED",
    HarvestNotReady = "HARVEST_NOT_READY",
    HttpGetAuthorizeNotSupported = "HTTP_GET_AUTHORIZE_NOT_SUPPORTED",
    InstanceVersionMismatch = "INSTANCE_VERSION_MISMATCH",
    InternalServerError = "INTERNAL_SERVER_ERROR",
    InvalidAclsFormat = "INVALID_ACLS_FORMAT",
    InvalidApiOrigin = "INVALID_API_ORIGIN",
    InvalidAuthToken = "INVALID_AUTH_TOKEN",
    InvalidBotFlag = "INVALID_BOT_FLAG",
    InvalidCaptcha = "INVALID_CAPTCHA",
    InvalidChannelTypeForCall = "INVALID_CHANNEL_TYPE_FOR_CALL",
    InvalidChannelType = "INVALID_CHANNEL_TYPE",
    InvalidClient = "INVALID_CLIENT",
    InvalidClientSecret = "INVALID_CLIENT_SECRET",
    InvalidDsaReportTarget = "INVALID_DSA_REPORT_TARGET",
    InvalidDsaTicket = "INVALID_DSA_TICKET",
    InvalidDsaVerificationCode = "INVALID_DSA_VERIFICATION_CODE",
    InvalidDecryptedJson = "INVALID_DECRYPTED_JSON",
    InvalidEphemeralKey = "INVALID_EPHEMERAL_KEY",
    InvalidFlagsFormat = "INVALID_FLAGS_FORMAT",
    InvalidIv = "INVALID_IV",
    InvalidFormBody = "INVALID_FORM_BODY",
    InvalidGrant = "INVALID_GRANT",
    InvalidHandoffCode = "INVALID_HANDOFF_CODE",
    InvalidPackType = "INVALID_PACK_TYPE",
    InvalidPermissionsInteger = "INVALID_PERMISSIONS_INTEGER",
    InvalidPermissionsNegative = "INVALID_PERMISSIONS_NEGATIVE",
    InvalidPhoneNumber = "INVALID_PHONE_NUMBER",
    InvalidPhoneVerificationCode = "INVALID_PHONE_VERIFICATION_CODE",
    InvalidRedirectUri = "INVALID_REDIRECT_URI",
    InvalidRequest = "INVALID_REQUEST",
    InvalidResponseTypeForNonBot = "INVALID_RESPONSE_TYPE_FOR_NON_BOT",
    InvalidScope = "INVALID_SCOPE",
    InvalidStreamKeyFormat = "INVALID_STREAM_KEY_FORMAT",
    InvalidStreamThumbnailPayload = "INVALID_STREAM_THUMBNAIL_PAYLOAD",
    InvalidSudoToken = "INVALID_SUDO_TOKEN",
    InvalidSuspiciousFlagsFormat = "INVALID_SUSPICIOUS_FLAGS_FORMAT",
    InvalidSystemFlag = "INVALID_SYSTEM_FLAG",
    InvalidTimestamp = "INVALID_TIMESTAMP",
    InvalidToken = "INVALID_TOKEN",
    InvalidWebauthnAuthenticationCounter = "INVALID_WEBAUTHN_AUTHENTICATION_COUNTER",
    InvalidWebauthnCredentialCounter = "INVALID_WEBAUTHN_CREDENTIAL_COUNTER",
    InvalidWebauthnCredential = "INVALID_WEBAUTHN_CREDENTIAL",
    InvalidWebauthnPublicKeyFormat = "INVALID_WEBAUTHN_PUBLIC_KEY_FORMAT",
    InvitesDisabled = "INVITES_DISABLED",
    IpAuthorizationRequired = "IP_AUTHORIZATION_REQUIRED",
    IpAuthorizationResendCooldown = "IP_AUTHORIZATION_RESEND_COOLDOWN",
    IpAuthorizationResendLimitExceeded = "IP_AUTHORIZATION_RESEND_LIMIT_EXCEEDED",
    GlobalIpBanned = "GLOBAL_IP_BANNED",
    GlobalIpTemporarilyBanned = "GLOBAL_IP_TEMPORARILY_BANNED",
    IpBanned = "IP_BANNED",
    ResidentialProxyBlocked = "RESIDENTIAL_PROXY_BLOCKED",
    TorBlocked = "TOR_BLOCKED",
    MaxAnimatedEmojis = "MAX_ANIMATED_EMOJIS",
    MaxApplications = "MAX_APPLICATIONS",
    MaxBookmarks = "MAX_BOOKMARKS",
    MaxCategoryChannels = "MAX_CATEGORY_CHANNELS",
    MaxEmojis = "MAX_EMOJIS",
    MaxFavoriteMemes = "MAX_FAVORITE_MEMES",
    MaxFriends = "MAX_FRIENDS",
    MaxGroupDmRecipients = "MAX_GROUP_DM_RECIPIENTS",
    MaxGroupDms = "MAX_GROUP_DMS",
    GroupDmRecipientsNotAddable = "GROUP_DM_RECIPIENTS_NOT_ADDABLE",
    MaxGuildChannels = "MAX_GUILD_CHANNELS",
    MaxGuildMembers = "MAX_GUILD_MEMBERS",
    MaxGuildRoles = "MAX_GUILD_ROLES",
    MaxGuilds = "MAX_GUILDS",
    NewAccountGuildJoinRateLimited = "NEW_ACCOUNT_GUILD_JOIN_RATE_LIMITED",
    MaxInvites = "MAX_INVITES",
    MaxPackExpressions = "MAX_PACK_EXPRESSIONS",
    MaxPacks = "MAX_PACKS",
    MaxPinsPerChannel = "MAX_PINS_PER_CHANNEL",
    MessageTotalAttachmentSizeTooLarge = "MESSAGE_TOTAL_ATTACHMENT_SIZE_TOO_LARGE",
    MaxReactions = "MAX_REACTIONS",
    MaxStickers = "MAX_STICKERS",
    MaxWebhooksPerChannel = "MAX_WEBHOOKS_PER_CHANNEL",
    MaxWebhooksPerGuild = "MAX_WEBHOOKS_PER_GUILD",
    MaxWebhooks = "MAX_WEBHOOKS",
    NcmecAlreadySubmitted = "NCMEC_ALREADY_SUBMITTED",
    NcmecSubmissionFailed = "NCMEC_SUBMISSION_FAILED",
    MediaMetadataError = "MEDIA_METADATA_ERROR",
    MethodNotAllowed = "METHOD_NOT_ALLOWED",
    MissingAccess = "MISSING_ACCESS",
    MissingAcl = "MISSING_ACL",
    MissingAuthorization = "MISSING_AUTHORIZATION",
    MissingClientSecret = "MISSING_CLIENT_SECRET",
    MissingEphemeralKey = "MISSING_EPHEMERAL_KEY",
    MissingIv = "MISSING_IV",
    MissingOauthFields = "MISSING_OAUTH_FIELDS",
    MissingOauthScope = "MISSING_OAUTH_SCOPE",
    MissingPermissions = "MISSING_PERMISSIONS",
    MissingRedirectUri = "MISSING_REDIRECT_URI",
    NoActiveCall = "NO_ACTIVE_CALL",
    NoActiveSubscription = "NO_ACTIVE_SUBSCRIPTION",
    NotFound = "NOT_FOUND",
    NotImplemented = "NOT_IMPLEMENTED",
    NoPasskeysRegistered = "NO_PASSKEYS_REGISTERED",
    NoPendingDeletion = "NO_PENDING_DELETION",
    NoUsersWithFluxertagExist = "NO_USERS_WITH_FLUXERTAG_EXIST",
    NoVisionarySlotsAvailable = "NO_VISIONARY_SLOTS_AVAILABLE",
    NotABotApplication = "NOT_A_BOT_APPLICATION",
    NotFriendsWithUser = "NOT_FRIENDS_WITH_USER",
    NotOwnerOfAdminApiKey = "NOT_OWNER_OF_ADMIN_API_KEY",
    NsfwContentAgeRestricted = "NSFW_CONTENT_AGE_RESTRICTED",
    NsfwEmojiStickerBlocked = "NSFW_EMOJI_STICKER_BLOCKED",
    PackAccessDenied = "PACK_ACCESS_DENIED",
    PasskeyAuthenticationFailed = "PASSKEY_AUTHENTICATION_FAILED",
    PasskeysDisabled = "PASSKEYS_DISABLED",
    PhoneAddNotEligible = "PHONE_ADD_NOT_ELIGIBLE",
    PhoneAlreadyUsed = "PHONE_ALREADY_USED",
    PhoneRateLimitExceeded = "PHONE_RATE_LIMIT_EXCEEDED",
    PhoneVerificationRequired = "PHONE_VERIFICATION_REQUIRED",
    PremiumPurchaseBlocked = "PREMIUM_PURCHASE_BLOCKED",
    PreviewMustBeJpeg = "PREVIEW_MUST_BE_JPEG",
    ProcessingFailed = "PROCESSING_FAILED",
    RateLimited = "RATE_LIMITED",
    RedirectUriRequiredForNonBot = "REDIRECT_URI_REQUIRED_FOR_NON_BOT",
    ReportAlreadyResolved = "REPORT_ALREADY_RESOLVED",
    ReportBanned = "REPORT_BANNED",
    ResponseValidationError = "RESPONSE_VALIDATION_ERROR",
    ResourceLocked = "RESOURCE_LOCKED",
    ServiceUnavailable = "SERVICE_UNAVAILABLE",
    SessionTokenMismatch = "SESSION_TOKEN_MISMATCH",
    SingleCommunityCannotCreateGuilds = "SINGLE_COMMUNITY_CANNOT_CREATE_GUILDS",
    SingleCommunityCannotDelete = "SINGLE_COMMUNITY_CANNOT_DELETE",
    SingleCommunityCannotLeave = "SINGLE_COMMUNITY_CANNOT_LEAVE",
    InstancePolicyTransitionNotAllowed = "INSTANCE_POLICY_TRANSITION_NOT_ALLOWED",
    SlowmodeRateLimited = "SLOWMODE_RATE_LIMITED",
    SmsVerificationUnavailable = "SMS_VERIFICATION_UNAVAILABLE",
    SsoRequired = "SSO_REQUIRED",
    StreamKeyChannelMismatch = "STREAM_KEY_CHANNEL_MISMATCH",
    StreamKeyScopeMismatch = "STREAM_KEY_SCOPE_MISMATCH",
    StreamThumbnailPayloadEmpty = "STREAM_THUMBNAIL_PAYLOAD_EMPTY",
    StripeError = "STRIPE_ERROR",
    StripeGiftRedemptionInProgress = "STRIPE_GIFT_REDEMPTION_IN_PROGRESS",
    StripeInvalidProduct = "STRIPE_INVALID_PRODUCT",
    StripeInvalidProductConfiguration = "STRIPE_INVALID_PRODUCT_CONFIGURATION",
    StripeNoActiveSubscription = "STRIPE_NO_ACTIVE_SUBSCRIPTION",
    StripeNoPurchaseHistory = "STRIPE_NO_PURCHASE_HISTORY",
    StripeRefundOutsideWindow = "STRIPE_REFUND_OUTSIDE_WINDOW",
    StripeRefundCooldownActive = "STRIPE_REFUND_COOLDOWN_ACTIVE",
    StripeNoSubscription = "STRIPE_NO_SUBSCRIPTION",
    StripePaymentNotAvailable = "STRIPE_PAYMENT_NOT_AVAILABLE",
    StripeSubscriptionAlreadyCanceling = "STRIPE_SUBSCRIPTION_ALREADY_CANCELING",
    StripeSubscriptionNotCanceling = "STRIPE_SUBSCRIPTION_NOT_CANCELING",
    StripeSubscriptionPeriodEndMissing = "STRIPE_SUBSCRIPTION_PERIOD_END_MISSING",
    StripeWebhookNotAvailable = "STRIPE_WEBHOOK_NOT_AVAILABLE",
    StripeWebhookSignatureInvalid = "STRIPE_WEBHOOK_SIGNATURE_INVALID",
    StripeWebhookSignatureMissing = "STRIPE_WEBHOOK_SIGNATURE_MISSING",
    DirectMessagesDisabled = "DIRECT_MESSAGES_DISABLED",
    DonationAmountInvalid = "DONATION_AMOUNT_INVALID",
    DonationMagicLinkExpired = "DONATION_MAGIC_LINK_EXPIRED",
    DonationMagicLinkInvalid = "DONATION_MAGIC_LINK_INVALID",
    DonationMagicLinkUsed = "DONATION_MAGIC_LINK_USED",
    DonorNotFound = "DONOR_NOT_FOUND",
    SudoModeRequired = "SUDO_MODE_REQUIRED",
    TagAlreadyTaken = "TAG_ALREADY_TAKEN",
    TemporaryInviteRequiresPresence = "TEMPORARY_INVITE_REQUIRES_PRESENCE",
    TestHarnessDisabled = "TEST_HARNESS_DISABLED",
    TestHarnessForbidden = "TEST_HARNESS_FORBIDDEN",
    TwoFaNotEnabled = "TWO_FA_NOT_ENABLED",
    TwoFactorRequired = "TWO_FACTOR_REQUIRED",
    Unauthorized = "UNAUTHORIZED",
    UnclaimedAccountCannotAcceptFriendRequests = "UNCLAIMED_ACCOUNT_CANNOT_ACCEPT_FRIEND_REQUESTS",
    UnclaimedAccountCannotAddReactions = "UNCLAIMED_ACCOUNT_CANNOT_ADD_REACTIONS",
    UnclaimedAccountCannotCreateApplications = "UNCLAIMED_ACCOUNT_CANNOT_CREATE_APPLICATIONS",
    UnclaimedAccountCannotCreateGuilds = "UNCLAIMED_ACCOUNT_CANNOT_CREATE_GUILDS",
    UnclaimedAccountCannotJoinGroupDms = "UNCLAIMED_ACCOUNT_CANNOT_JOIN_GROUP_DMS",
    UnclaimedAccountCannotJoinOneOnOneVoiceCalls = "UNCLAIMED_ACCOUNT_CANNOT_JOIN_ONE_ON_ONE_VOICE_CALLS",
    UnclaimedAccountCannotJoinVoiceChannels = "UNCLAIMED_ACCOUNT_CANNOT_JOIN_VOICE_CHANNELS",
    UnclaimedAccountCannotMakePurchases = "UNCLAIMED_ACCOUNT_CANNOT_MAKE_PURCHASES",
    UnclaimedAccountCannotSendDirectMessages = "UNCLAIMED_ACCOUNT_CANNOT_SEND_DIRECT_MESSAGES",
    UnclaimedAccountCannotSendFriendRequests = "UNCLAIMED_ACCOUNT_CANNOT_SEND_FRIEND_REQUESTS",
    UnclaimedAccountCannotSendMessages = "UNCLAIMED_ACCOUNT_CANNOT_SEND_MESSAGES",
    UnclaimedAccountCannotSubmitReports = "UNCLAIMED_ACCOUNT_CANNOT_SUBMIT_REPORTS",
    UnknownChannel = "UNKNOWN_CHANNEL",
    UnknownEmoji = "UNKNOWN_EMOJI",
    UnknownFavoriteMeme = "UNKNOWN_FAVORITE_MEME",
    UnknownGiftCode = "UNKNOWN_GIFT_CODE",
    UnknownGuild = "UNKNOWN_GUILD",
    UnknownHarvest = "UNKNOWN_HARVEST",
    UnknownInvite = "UNKNOWN_INVITE",
    UnknownMember = "UNKNOWN_MEMBER",
    UnknownMessage = "UNKNOWN_MESSAGE",
    UnknownPack = "UNKNOWN_PACK",
    UnknownReport = "UNKNOWN_REPORT",
    UnknownRole = "UNKNOWN_ROLE",
    UnknownSticker = "UNKNOWN_STICKER",
    UnknownSuspiciousFlag = "UNKNOWN_SUSPICIOUS_FLAG",
    UnknownUserFlag = "UNKNOWN_USER_FLAG",
    UnknownUser = "UNKNOWN_USER",
    UnknownVoiceRegion = "UNKNOWN_VOICE_REGION",
    UnknownVoiceServer = "UNKNOWN_VOICE_SERVER",
    UnknownWebauthnCredential = "UNKNOWN_WEBAUTHN_CREDENTIAL",
    UnknownApplication = "UNKNOWN_APPLICATION",
    UnknownWebhook = "UNKNOWN_WEBHOOK",
    UnfurlStreamTooLarge = "UNFURL_STREAM_TOO_LARGE",
    UnsupportedResponseType = "UNSUPPORTED_RESPONSE_TYPE",
    UsernameNotAvailable = "USERNAME_NOT_AVAILABLE",
    UpdateFailed = "UPDATE_FAILED",
    UserBannedFromGuild = "USER_BANNED_FROM_GUILD",
    UserIpBannedFromGuild = "USER_IP_BANNED_FROM_GUILD",
    UserNotInVoice = "USER_NOT_IN_VOICE",
    UserOwnsGuilds = "USER_OWNS_GUILDS",
    ValidationError = "VALIDATION_ERROR",
    VoiceChannelFull = "VOICE_CHANNEL_FULL",
    WebauthnCredentialLimitReached = "WEBAUTHN_CREDENTIAL_LIMIT_REACHED",
    AgeVerificationAlreadyVerified = "AGE_VERIFICATION_ALREADY_VERIFIED",
    AgeVerificationInvalidCardType = "AGE_VERIFICATION_INVALID_CARD_TYPE"
}
/** Standard API error response body. */
interface APIErrorBody {
    /** Error code (see {@link APIErrorCode}). */
    code: APIErrorCode | string;
    /** Human-readable error message. */
    message: string;
    /** Field-level validation errors. */
    errors?: Array<{
        path: string;
        message: string;
        code?: string;
    }>;
}
/** Rate limit error response body (includes retry_after). */
interface RateLimitErrorBody extends APIErrorBody {
    /** Rate limit error code. */
    code: 'RATE_LIMITED';
    /** Seconds to wait before retrying. */
    retry_after: number;
    /** Whether this is a global rate limit. */
    global?: boolean;
}

/**
 * Response from GET /gateway/bot
 * url: WebSocket URL, session_start_limit: rate limit info in ms
 */
interface APIGatewayBotResponse {
    url: string;
    shards: number;
    session_start_limit: {
        total: number;
        remaining: number;
        reset_after: number;
        max_concurrency: number;
    };
}

/**
 * Response from `GET /.well-known/fluxer` (unauthenticated instance discovery).
 * Aligns with OpenAPI `WellKnownFluxerResponse`.
 */
/** Endpoint URLs for a Fluxer instance. */
interface APIInstanceEndpoints {
    /** Base URL for authenticated API requests (origin, no `/vN` suffix). */
    api: string;
    /** Base URL for client API requests. */
    api_client: string;
    /** Base URL for public API requests. */
    api_public: string;
    /** WebSocket URL for the gateway. */
    gateway: string;
    /** Base URL for the media proxy (avatars, icons, emojis, etc.). */
    media: string;
    /** Base URL for static assets (default avatars). */
    static_cdn: string;
    /** Base URL for the marketing website. */
    marketing: string;
    /** Base URL for the admin panel. */
    admin: string;
    /** Base URL for invite links. */
    invite: string;
    /** Base URL for gift links. */
    gift: string;
    /** Base URL for the web application. */
    webapp: string;
}
/** Captcha configuration from instance discovery. */
interface APIInstanceCaptcha {
    provider: string;
    hcaptcha_site_key: string | null;
    turnstile_site_key: string | null;
}
/** Feature flags from instance discovery. */
interface APIInstanceFeatures {
    voice_enabled: boolean;
    stripe_enabled: boolean;
    self_hosted: boolean;
    presigned_attachment_uploads: boolean;
    emails_enabled: boolean;
}
/** GIF provider configuration. */
interface APIInstanceGif {
    provider: string;
    display_name: string;
    attribution_required: boolean;
}
/** SSO configuration. */
interface APIInstanceSso {
    enabled: boolean;
    enforced: boolean;
    display_name: string | null;
    redirect_uri: string;
}
/** Registration policy. */
interface APIInstanceRegistration {
    mode: 'open' | 'approval' | 'closed';
    admin_registration_urls_enabled: boolean;
}
/** Community topology. */
interface APIInstanceCommunity {
    single_community: boolean;
    single_community_guild_id: string | null;
    direct_messages_disabled: boolean;
}
/** Optional third-party service toggles. */
interface APIInstanceServices {
    gif_enabled: boolean;
    youtube_enabled: boolean;
    bluesky_enabled: boolean;
}
/** Limit rule from instance discovery. */
interface APIInstanceLimitRule {
    id: string;
    filters?: {
        traits?: string[];
        guildFeatures?: string[];
    };
    overrides: Record<string, number>;
}
/** Limit configuration. */
interface APIInstanceLimits {
    version: 2;
    traitDefinitions: string[];
    rules: APIInstanceLimitRule[];
    defaultsHash: string;
}
/** Push notification configuration. */
interface APIInstancePush {
    public_vapid_key: string | null;
}
/** Public branding / legal / setup metadata. */
interface APIInstanceAppPublic {
    branding?: {
        product_name?: string;
        icon_url?: string | null;
        symbol_url?: string | null;
        logo_url?: string | null;
        wordmark_url?: string | null;
        favicon_url?: string | null;
        theme_color?: string | null;
    };
    setup?: {
        configured?: boolean;
        admin_url?: string | null;
    };
    legal?: {
        terms_url?: string | null;
        privacy_url?: string | null;
    };
    registration?: {
        collect_date_of_birth?: boolean;
    };
}
/**
 * Full instance discovery document (`WellKnownFluxerResponse`).
 * Prefer {@link APIInstanceEndpoints} when only URLs are needed.
 */
/**
 * Full instance discovery document (`WellKnownFluxerResponse`).
 * Nested sections are required on the wire; discovery helpers may fill defaults when absent.
 */
interface APIInstance {
    api_code_version: number;
    endpoints: APIInstanceEndpoints;
    captcha: APIInstanceCaptcha;
    features: APIInstanceFeatures;
    gif: APIInstanceGif;
    sso: APIInstanceSso;
    registration: APIInstanceRegistration;
    community: APIInstanceCommunity;
    services: APIInstanceServices;
    limits: APIInstanceLimits;
    push: APIInstancePush;
    app_public: APIInstanceAppPublic;
}
/** @deprecated Use {@link APIInstance} — alias kept for clarity with OpenAPI naming. */
type APIWellKnownFluxer = APIInstance;

/**
 * Invite type discriminator (OpenAPI InviteType).
 * - `Guild` — invite to a guild
 * - `GroupDM` — invite to a group DM
 * - `EmojiPack` — invite to clone an emoji pack
 * - `StickerPack` — invite to clone a sticker pack
 */
declare enum InviteType {
    Guild = 0,
    GroupDM = 1,
    EmojiPack = 2,
    StickerPack = 3
}
/** Partial guild embedded on guild invites. */
interface APIGuildPartial {
    /** Guild ID. */
    id: Snowflake;
    /** Guild name. */
    name: string;
    /** Guild icon hash. */
    icon?: string | null;
    /** Guild banner hash. */
    banner?: string | null;
    /** Invite splash hash. */
    splash?: string | null;
    /** Guild features. */
    features?: string[];
}
/** Pack summary embedded on pack invites. */
interface APIPackInviteInfo {
    /** Pack ID. */
    id: Snowflake;
    /** Pack name. */
    name: string;
    /** Pack description. */
    description?: string | null;
    /** Pack type (emoji or sticker). */
    type: 'emoji' | 'sticker';
    /** Creator user ID. */
    creator_id: Snowflake;
    /** ISO-8601 timestamp when the pack was created. */
    created_at: string;
    /** ISO-8601 timestamp when the pack was last updated. */
    updated_at: string;
    /** Pack creator user object. */
    creator: APIUser;
}
/** Request body for POST /channels/{id}/invites (ChannelInviteCreateRequest). */
interface ChannelInviteCreateRequest {
    /** Maximum number of uses (null = unlimited). */
    max_uses?: number | null;
    /** Expiration in seconds (null = never). */
    max_age?: number | null;
    /** Whether to create a unique code (no reuse). */
    unique?: boolean | null;
    /** Whether membership is temporary (kicked on disconnect). */
    temporary?: boolean | null;
}
/** Request body for POST /packs/{id}/invites (PackInviteCreateRequest). */
interface PackInviteCreateRequest {
    /** Maximum number of uses (null = unlimited). */
    max_uses?: number | null;
    /** Expiration in seconds (null = never). */
    max_age?: number | null;
    /** Whether to create a unique code (no reuse). */
    unique?: boolean | null;
}
/** Shared fields on all invite types. */
interface InviteShared {
    /** Invite code (the part after `/invite/`). */
    code: string;
    /** User who created the invite. */
    inviter?: APIUser | null;
    /** ISO-8601 expiration timestamp. */
    expires_at?: string | null;
    /** Whether membership is temporary (kicked on disconnect). Always present on REST responses; may be absent on gateway partials. */
    temporary?: boolean;
    /** ISO-8601 creation timestamp. */
    created_at?: string;
    /** Current use count. */
    uses?: number;
    /** Maximum use count (null = unlimited). */
    max_uses?: number;
    /** Maximum age in seconds (null = never). */
    max_age?: number;
}
/** Guild invite (type 0). */
interface APIGuildInvite extends InviteShared {
    type: InviteType.Guild;
    /** Guild being invited to. */
    guild: APIGuildPartial;
    /** Channel the invite targets. */
    channel: APIChannelPartial;
    /**
     * Total guild member count.
     * Required on REST `GuildInviteResponse`; may be omitted on gateway INVITE_CREATE partials.
     */
    member_count?: number;
    /**
     * Online member count.
     * Required on REST `GuildInviteResponse`; may be omitted on gateway INVITE_CREATE partials.
     */
    presence_count?: number;
}
/** Group DM invite (type 1). */
interface APIGroupDmInvite extends InviteShared {
    type: InviteType.GroupDM;
    /** Group DM channel. */
    channel: APIChannelPartial;
    /** Total member count. */
    member_count?: number;
}
/** Pack invite (type 2 or 3). */
interface APIPackInvite extends InviteShared {
    type: InviteType.EmojiPack | InviteType.StickerPack;
    /** Pack being invited to clone. */
    pack: APIPackInviteInfo;
}
/** Union of all invite types (GET /invites/{code}). */
type APIInviteResponse = APIGuildInvite | APIGroupDmInvite | APIPackInvite;
/** Guild invite with full metadata (list/metadata responses). */
type APIGuildInviteMetadata = APIGuildInvite & Required<Pick<InviteShared, 'created_at' | 'uses' | 'max_uses' | 'max_age' | 'temporary'>> & Required<Pick<APIGuildInvite, 'member_count' | 'presence_count'>>;
/** Group DM invite with full metadata (list/metadata responses). */
type APIGroupDmInviteMetadata = APIGroupDmInvite & Required<Pick<InviteShared, 'created_at' | 'uses' | 'max_uses' | 'max_age' | 'temporary'>> & Required<Pick<APIGroupDmInvite, 'member_count'>>;
/** Pack invite with full metadata (list/metadata responses). */
type APIPackInviteMetadata = APIPackInvite & Required<Pick<InviteShared, 'created_at' | 'uses' | 'max_uses' | 'temporary'>>;
/** Union of all invite metadata types. */
type APIInviteMetadata = APIGuildInviteMetadata | APIGroupDmInviteMetadata | APIPackInviteMetadata;
/** Any invite payload (REST, list, or gateway INVITE_CREATE). */
type APIInvite = APIInviteResponse;
/** Type guard to check if invite is a guild invite. */
declare function isGuildInvite(invite: APIInvite): invite is APIGuildInvite;
/** Type guard to check if invite is a group DM invite. */
declare function isGroupDmInvite(invite: APIInvite): invite is APIGroupDmInvite;
/** Type guard to check if invite is a pack invite. */
declare function isPackInvite(invite: APIInvite): invite is APIPackInvite;

/**
 * Expression pack kind (OpenAPI PackType).
 * - `emoji` — emoji pack
 * - `sticker` — sticker pack
 */
type PackType = 'emoji' | 'sticker';
/** Request body for POST /packs (create pack). */
interface APIPackCreateRequest {
    /** Pack name. */
    name: string;
    /** Pack description. */
    description?: string | null;
}
/** Request body for PATCH /packs/{id} (update pack). */
interface APIPackUpdateRequest {
    /** Pack name. */
    name?: string;
    /** Pack description. */
    description?: string | null;
}
/** Pack summary from GET /packs or pack list responses. */
interface APIPackSummary {
    /** Pack ID. */
    id: Snowflake;
    /** Pack name. */
    name: string;
    /** Pack description. */
    description: string | null;
    /** Pack type (emoji or sticker). */
    type: PackType;
    /** Creator user ID. */
    creator_id: Snowflake;
    /** ISO-8601 timestamp when created. */
    created_at: string;
    /** ISO-8601 timestamp when last updated. */
    updated_at: string;
    /** ISO-8601 timestamp when installed (if installed). */
    installed_at?: string;
}
/** Dashboard section for one pack type (emoji or sticker). */
interface APIPackDashboardSection {
    /** Limit of installed packs. */
    installed_limit: number;
    /** Limit of created packs. */
    created_limit: number;
    /** Installed packs. */
    installed: APIPackSummary[];
    /** Created packs. */
    created: APIPackSummary[];
}
/** Response from GET /packs/dashboard (user's pack dashboard). */
interface APIPackDashboard {
    /** Emoji pack section. */
    emoji: APIPackDashboardSection;
    /** Sticker pack section. */
    sticker: APIPackDashboardSection;
}

/**
 * Sudo / MFA verification body used by sensitive DELETE/POST operations
 * (guild delete, leave with delete_messages, group DM leave, etc.).
 */
interface APISudoVerification {
    password?: string;
    mfa_method?: 'totp' | 'webauthn';
    mfa_code?: string;
    webauthn_response?: Record<string, unknown>;
    webauthn_challenge?: string;
}

/** External connection on a user profile (Bluesky, custom domain, etc.). */
interface APIUserConnection {
    id: string;
    type: string;
    name: string;
    verified: boolean;
    visibility_flags: number;
    sort_order: number;
}
/** WebAuthn passkey credential metadata. */
interface APIWebAuthnCredential {
    id: string;
    name: string;
    created_at: string;
    last_used_at: string | null;
}
/** USER_CONNECTIONS_UPDATE gateway payload. */
interface APIUserConnectionsUpdate {
    connections: APIUserConnection[];
}
/** Guild member snapshot included with bulk reaction adds. */
interface GatewayReactionMemberSnapshot {
    user?: {
        id: Snowflake;
    };
    roles?: Snowflake[];
    nick?: string | null;
    joined_at?: string;
    avatar?: string | null;
    [key: string]: unknown;
}

/**
 * Voice state as returned by Fluxer (GUILD_CREATE.voice_states, VOICE_STATE_UPDATE).
 * Matches fluxer `VoiceStateResponse`.
 */
interface APIVoiceState {
    guild_id?: Snowflake | null;
    channel_id: Snowflake | null;
    user_id: Snowflake;
    connection_id?: string | null;
    session_id?: string;
    member?: APIGuildMember;
    mute: boolean;
    deaf: boolean;
    self_mute: boolean;
    self_deaf: boolean;
    suppress?: boolean;
    self_video?: boolean;
    self_stream?: boolean;
    is_mobile?: boolean;
    viewer_stream_keys?: string[] | null;
    version?: number;
    /** True when the client advertised E2EE support in IDENTIFY. */
    e2ee_capable?: boolean;
}
/** Minimal voice-state stub used by `@fluxerjs/voice` before a full wire payload exists. */
type APIVoiceStateStub = Partial<APIVoiceState> & Pick<APIVoiceState, 'channel_id' | 'user_id'>;

/**
 * Gateway dispatch event names sent by the Fluxer gateway.
 * Sourced from fluxer_gateway event_atoms.erl. Use for type-safe event handling.
 *
 * @example
 * ```ts
 * if (payload.t === GatewayDispatchEvents.MessageCreate) {
 *   // handle message
 * }
 * ```
 */
declare const GatewayDispatchEvents: {
    /** Initial connection established; contains user, guilds, and session state. */
    readonly Ready: "READY";
    /** Connection resumed after disconnect; sequence restored. */
    readonly Resumed: "RESUMED";
    /** User's active sessions list replaced (e.g. new login). */
    readonly SessionsReplace: "SESSIONS_REPLACE";
    /** Current user's profile was updated. */
    readonly UserUpdate: "USER_UPDATE";
    /** User settings changed (theme, locale, etc.). */
    readonly UserSettingsUpdate: "USER_SETTINGS_UPDATE";
    /** Per-guild user settings changed (notifications, etc.). */
    readonly UserGuildSettingsUpdate: "USER_GUILD_SETTINGS_UPDATE";
    /** User's linked external connections changed. */
    readonly UserConnectionsUpdate: "USER_CONNECTIONS_UPDATE";
    /** User's WebAuthn credentials changed. */
    readonly WebAuthnCredentialsUpdate: "WEBAUTHN_CREDENTIALS_UPDATE";
    /** Pinned DM order changed. */
    readonly UserPinnedDmsUpdate: "USER_PINNED_DMS_UPDATE";
    /** Note on another user changed. */
    readonly UserNoteUpdate: "USER_NOTE_UPDATE";
    /** Recent mention was cleared. */
    readonly RecentMentionDelete: "RECENT_MENTION_DELETE";
    /** Message was saved (bookmarked). */
    readonly SavedMessageCreate: "SAVED_MESSAGE_CREATE";
    /** Saved message was unsaved. */
    readonly SavedMessageDelete: "SAVED_MESSAGE_DELETE";
    /** Auth session changed (login/logout on another client). */
    readonly AuthSessionChange: "AUTH_SESSION_CHANGE";
    /** User's presence (status, activity) updated. */
    readonly PresenceUpdate: "PRESENCE_UPDATE";
    /** Multiple presence updates in one dispatch. */
    readonly PresenceUpdateBulk: "PRESENCE_UPDATE_BULK";
    /** Bot joined a guild or guild data became available. */
    readonly GuildCreate: "GUILD_CREATE";
    /** Guild was updated. */
    readonly GuildUpdate: "GUILD_UPDATE";
    /** Bot left or was removed from a guild. */
    readonly GuildDelete: "GUILD_DELETE";
    /** Member joined a guild. */
    readonly GuildMemberAdd: "GUILD_MEMBER_ADD";
    /** Member was updated (roles, nickname, etc.). */
    readonly GuildMemberUpdate: "GUILD_MEMBER_UPDATE";
    /** Member left or was removed from a guild. */
    readonly GuildMemberRemove: "GUILD_MEMBER_REMOVE";
    /** Chunk of guild members (from request_guild_members). */
    readonly GuildMembersChunk: "GUILD_MEMBERS_CHUNK";
    /** Member list view updated (lazy loading). */
    readonly GuildMemberListUpdate: "GUILD_MEMBER_LIST_UPDATE";
    /** Guild sync state (passive/lazy loading). */
    readonly GuildSync: "GUILD_SYNC";
    /** Member and online counts for one or more guilds. */
    readonly GuildCountsUpdate: "GUILD_COUNTS_UPDATE";
    /** Member and online counts for guild channels. */
    readonly ChannelMemberCountsUpdate: "CHANNEL_MEMBER_COUNTS_UPDATE";
    /** Role was created. */
    readonly GuildRoleCreate: "GUILD_ROLE_CREATE";
    /** Role was updated. */
    readonly GuildRoleUpdate: "GUILD_ROLE_UPDATE";
    /** Multiple roles updated at once. */
    readonly GuildRoleUpdateBulk: "GUILD_ROLE_UPDATE_BULK";
    /** Role was deleted. */
    readonly GuildRoleDelete: "GUILD_ROLE_DELETE";
    /** Guild emojis changed. */
    readonly GuildEmojisUpdate: "GUILD_EMOJIS_UPDATE";
    /** Guild stickers changed. */
    readonly GuildStickersUpdate: "GUILD_STICKERS_UPDATE";
    /** User was banned from a guild. */
    readonly GuildBanAdd: "GUILD_BAN_ADD";
    /** User was unbanned from a guild. */
    readonly GuildBanRemove: "GUILD_BAN_REMOVE";
    /** New guild audit log entry. */
    readonly GuildAuditLogEntryCreate: "GUILD_AUDIT_LOG_ENTRY_CREATE";
    /** Channel was created. */
    readonly ChannelCreate: "CHANNEL_CREATE";
    /** Channel was updated. */
    readonly ChannelUpdate: "CHANNEL_UPDATE";
    /** Multiple channels updated at once. */
    readonly ChannelUpdateBulk: "CHANNEL_UPDATE_BULK";
    /** Channel was deleted. */
    readonly ChannelDelete: "CHANNEL_DELETE";
    /** Recipient added to group DM. */
    readonly ChannelRecipientAdd: "CHANNEL_RECIPIENT_ADD";
    /** Recipient removed from group DM. */
    readonly ChannelRecipientRemove: "CHANNEL_RECIPIENT_REMOVE";
    /** Pinned messages in a channel changed. */
    readonly ChannelPinsUpdate: "CHANNEL_PINS_UPDATE";
    /** User acknowledged viewing pinned messages. */
    readonly ChannelPinsAck: "CHANNEL_PINS_ACK";
    /** Passive/lazy-loaded entity updates. */
    readonly PassiveUpdates: "PASSIVE_UPDATES";
    /** Invite was created. */
    readonly InviteCreate: "INVITE_CREATE";
    /** Invite was deleted or expired. */
    readonly InviteDelete: "INVITE_DELETE";
    /** New message was sent. */
    readonly MessageCreate: "MESSAGE_CREATE";
    /** Message was edited. */
    readonly MessageUpdate: "MESSAGE_UPDATE";
    /** Message was deleted. */
    readonly MessageDelete: "MESSAGE_DELETE";
    /** Multiple messages were deleted. */
    readonly MessageDeleteBulk: "MESSAGE_DELETE_BULK";
    /** Reaction was added to a message. */
    readonly MessageReactionAdd: "MESSAGE_REACTION_ADD";
    /** Multiple reactions added to a message in one dispatch. */
    readonly MessageReactionAddMany: "MESSAGE_REACTION_ADD_MANY";
    /** Reaction was removed from a message. */
    readonly MessageReactionRemove: "MESSAGE_REACTION_REMOVE";
    /** All reactions were removed from a message. */
    readonly MessageReactionRemoveAll: "MESSAGE_REACTION_REMOVE_ALL";
    /** All reactions of an emoji were removed from a message. */
    readonly MessageReactionRemoveEmoji: "MESSAGE_REACTION_REMOVE_EMOJI";
    /** Message was acknowledged (read receipt). */
    readonly MessageAck: "MESSAGE_ACK";
    /** User started typing in a channel. */
    readonly TypingStart: "TYPING_START";
    /** Webhooks for a channel were updated. */
    readonly WebhooksUpdate: "WEBHOOKS_UPDATE";
    /** Relationship (friend, block) was added. */
    readonly RelationshipAdd: "RELATIONSHIP_ADD";
    /** Relationship was updated. */
    readonly RelationshipUpdate: "RELATIONSHIP_UPDATE";
    /** Relationship was removed. */
    readonly RelationshipRemove: "RELATIONSHIP_REMOVE";
    /** Voice state changed (join, leave, mute, etc.). */
    readonly VoiceStateUpdate: "VOICE_STATE_UPDATE";
    /** Acknowledgement for a voice state mutation. */
    readonly VoiceStateAck: "VOICE_STATE_ACK";
    /** Voice server allocation info (for connecting to voice). */
    readonly VoiceServerUpdate: "VOICE_SERVER_UPDATE";
    /** Entrance sound played when a user joins voice. */
    readonly EntranceSoundPlay: "ENTRANCE_SOUND_PLAY";
    /** Call was created. */
    readonly CallCreate: "CALL_CREATE";
    /** Call was updated. */
    readonly CallUpdate: "CALL_UPDATE";
    /** Call was ended. */
    readonly CallDelete: "CALL_DELETE";
    /** Favorite meme/media was added. */
    readonly FavoriteMemeCreate: "FAVORITE_MEME_CREATE";
    /** Favorite meme/media was updated. */
    readonly FavoriteMemeUpdate: "FAVORITE_MEME_UPDATE";
    /** Favorite meme/media was removed. */
    readonly FavoriteMemeDelete: "FAVORITE_MEME_DELETE";
};
/** Union of all gateway dispatch event names. */
type GatewayDispatchEventName = (typeof GatewayDispatchEvents)[keyof typeof GatewayDispatchEvents];

/**
 * Gateway wire error codes (Fluxer `GatewayErrorCodes`).
 * Seen on VOICE_STATE_ACK / gateway error payloads.
 */
declare const GatewayErrorCodes: {
    readonly DmInvalidChannelType: "DM_INVALID_CHANNEL_TYPE";
    readonly DmNotRecipient: "DM_NOT_RECIPIENT";
    readonly UnknownError: "UNKNOWN_ERROR";
    readonly VoiceChannelFull: "VOICE_CHANNEL_FULL";
    readonly VoiceChannelNotFound: "VOICE_CHANNEL_NOT_FOUND";
    readonly VoiceConnectionLimitReached: "VOICE_CONNECTION_LIMIT_REACHED";
    readonly VoiceConnectionNotFound: "VOICE_CONNECTION_NOT_FOUND";
    readonly VoiceGuildIdMissing: "VOICE_GUILD_ID_MISSING";
    readonly VoiceGuildNotFound: "VOICE_GUILD_NOT_FOUND";
    readonly VoiceInvalidChannelId: "VOICE_INVALID_CHANNEL_ID";
    readonly VoiceInvalidChannelType: "VOICE_INVALID_CHANNEL_TYPE";
    readonly VoiceInvalidGuildId: "VOICE_INVALID_GUILD_ID";
    readonly VoiceInvalidState: "VOICE_INVALID_STATE";
    readonly VoiceInvalidUserId: "VOICE_INVALID_USER_ID";
    readonly VoiceMemberNotFound: "VOICE_MEMBER_NOT_FOUND";
    readonly VoiceMemberTimedOut: "VOICE_MEMBER_TIMED_OUT";
    readonly VoiceMissingConnectionId: "VOICE_MISSING_CONNECTION_ID";
    readonly VoicePermissionDenied: "VOICE_PERMISSION_DENIED";
    readonly VoiceTokenFailed: "VOICE_TOKEN_FAILED";
    readonly VoiceUnclaimedAccount: "VOICE_UNCLAIMED_ACCOUNT";
    readonly VoiceUserMismatch: "VOICE_USER_MISMATCH";
    readonly VoiceUserNotInVoice: "VOICE_USER_NOT_IN_VOICE";
};
type GatewayErrorCode = (typeof GatewayErrorCodes)[keyof typeof GatewayErrorCodes];

/**
 * Gateway opcodes sent by the Fluxer gateway.
 * - `Dispatch` — event dispatch (t = event name, d = event data)
 * - `Heartbeat` — heartbeat ping/pong
 * - `Identify` — identify connection (initial handshake)
 * - `PresenceUpdate` — update presence (status, activity, custom status)
 * - `VoiceStateUpdate` — update voice state (join/leave/mute/deaf)
 * - `VoiceServerPing` — ping voice server
 * - `Resume` — resume connection after disconnect
 * - `Reconnect` — server requests reconnect
 * - `RequestGuildMembers` — request guild members chunk
 * - `InvalidSession` — session invalid, re-identify
 * - `Hello` — server hello (heartbeat_interval)
 * - `HeartbeatAck` — heartbeat acknowledged
 * - `GatewayError` — gateway error
 * - `LazyRequest` — lazy load guilds/channels
 * - `RequestGuildCounts` — request guild member/online counts
 * - `RequestChannelMemberCounts` — request per-channel member counts
 * @see https://docs.fluxer.app/gateway/opcodes
 */
declare enum GatewayOpcodes {
    Dispatch = 0,
    Heartbeat = 1,
    Identify = 2,
    PresenceUpdate = 3,
    VoiceStateUpdate = 4,
    VoiceServerPing = 5,
    Resume = 6,
    Reconnect = 7,
    RequestGuildMembers = 8,
    InvalidSession = 9,
    Hello = 10,
    HeartbeatAck = 11,
    GatewayError = 12,
    LazyRequest = 14,
    RequestGuildCounts = 15,
    RequestChannelMemberCounts = 16
}

/** Identify flags (Fluxer `GatewayIdentifyFlags`). Sent as `flags` on IDENTIFY. */
declare const GatewayIdentifyFlags: {
    readonly DebounceMessageReactions: number;
};
/** Identify payload (opcode 2) — initial handshake. */
interface GatewayIdentifyData {
    /** Bot or user token. */
    token: string;
    /** Client properties. */
    properties: {
        os: string;
        browser: string;
        device: string;
    };
    /**
     * Legacy Discord-style intents bitfield. Fluxer ignores this; prefer {@link flags}.
     * @deprecated Fluxer has no gateway intents — omit or send `0`.
     */
    intents?: number;
    /** {@link GatewayIdentifyFlags} bitfield. */
    flags?: number;
    /** Dispatch event names to suppress for this session. */
    ignored_events?: string[];
    /** Prefer hydrating this guild first after READY. */
    initial_guild_id?: Snowflake;
    /** Whether to use zlib compression. */
    compress?: boolean;
    /** Threshold for large guild offline member fetching. */
    large_threshold?: number;
    /** Shard info [shard_id, num_shards]. */
    shard?: [shardId: number, numShards: number];
    /** Initial presence. */
    presence?: GatewayPresenceUpdateData;
}
/** Resume payload (opcode 6) — reconnect with existing session. */
interface GatewayResumeData {
    /** Bot or user token. */
    token: string;
    /** Session ID from READY. */
    session_id: string;
    /** Last received sequence number. */
    seq: number;
}
/** Custom status object (Fluxer uses this root object rather than Discord-style activities array). */
interface GatewayCustomStatus {
    /** Status text. */
    text?: string | null;
    /** Unicode emoji name. */
    emoji_name?: string | null;
    /** Custom emoji ID. */
    emoji_id?: string | null;
}
/** Presence update payload (opcode 3) — update status/activity. */
interface GatewayPresenceUpdateData {
    /** Unix timestamp when the client went idle (null if active). */
    since?: number | null;
    /** Activities array (for compatibility; Fluxer also supports custom_status). */
    activities?: Array<{
        name: string;
        /** Activity type integer (no closed OpenAPI enum yet). */
        type: number;
        url?: string | null;
    }>;
    /** Custom status; set text (and optionally emoji) for bots. Passable on identify and via presence update. */
    custom_status?: GatewayCustomStatus | null;
    /** Status (online, idle, dnd, invisible). */
    status: 'online' | 'idle' | 'dnd' | 'invisible';
    /** Whether the client is AFK. */
    afk?: boolean;
}
/** Voice state update payload (opcode 4) — join/leave/mute/deaf. */
interface GatewayVoiceStateUpdateData {
    /** Guild ID. */
    guild_id: Snowflake;
    /** Channel ID (null to disconnect). */
    channel_id: Snowflake | null;
    /** Whether self-muted. */
    self_mute?: boolean;
    /** Whether self-deafened. */
    self_deaf?: boolean;
    /** Whether the user has video enabled (e.g. camera). */
    self_video?: boolean;
    /** Whether the user is screen sharing / streaming. */
    self_stream?: boolean;
    /** Connection ID from VoiceServerUpdate; required for updates when already in channel. */
    connection_id?: string | null;
}
/** Client request for guild member/online counts (opcode 15). */
interface GatewayRequestGuildCountsData {
    /** Guild IDs to fetch counts for. */
    guild_ids: Snowflake[];
    /** Optional nonce for matching response. */
    nonce?: string;
}
/** Client request for per-channel member counts (opcode 16). */
interface GatewayRequestChannelMemberCountsData {
    /** Guild ID. */
    guild_id: Snowflake;
    /** Single channel ID. */
    channel_id?: Snowflake;
    /** Multiple channel IDs. */
    channel_ids?: Snowflake[];
    /** Optional nonce for matching response. */
    nonce?: string;
}
/** Client request for guild members (opcode 8). */
interface GatewayRequestGuildMembersData {
    /** Single guild ID. */
    guild_id?: Snowflake;
    /** Multiple guild IDs. */
    guild_ids?: Snowflake[];
    /** Username prefix query (empty string = all, when not using user_ids). */
    query?: string;
    /** Max members to return (0 = no limit when using user_ids). */
    limit?: number;
    /** Specific user IDs to fetch. */
    user_ids?: Snowflake[];
    /** Whether to include presences in the chunk. */
    presences?: boolean;
    /** Optional nonce echoed in GUILD_MEMBERS_CHUNK. */
    nonce?: string;
}
/** Union of all client-to-gateway payloads. */
type GatewaySendPayload = {
    op: GatewayOpcodes.Identify;
    d: GatewayIdentifyData;
} | {
    op: GatewayOpcodes.Resume;
    d: GatewayResumeData;
} | {
    op: GatewayOpcodes.Heartbeat;
    d: number | null;
} | {
    op: GatewayOpcodes.PresenceUpdate;
    d: GatewayPresenceUpdateData;
} | {
    op: GatewayOpcodes.VoiceStateUpdate;
    d: GatewayVoiceStateUpdateData;
} | {
    op: GatewayOpcodes.RequestGuildMembers;
    d: GatewayRequestGuildMembersData;
} | {
    op: GatewayOpcodes.RequestGuildCounts;
    d: GatewayRequestGuildCountsData;
} | {
    op: GatewayOpcodes.RequestChannelMemberCounts;
    d: GatewayRequestChannelMemberCountsData;
};
/** Hello payload (opcode 10) — server hello with heartbeat interval. */
interface GatewayHelloData {
    /** Milliseconds between heartbeats. */
    heartbeat_interval: number;
}
/**
 * Guild snapshot in READY / GUILD_CREATE.
 * Fluxer nests guild metadata under `properties`; flat guild objects are also accepted.
 */
type GatewayGuildSnapshot = (APIGuild & {
    unavailable?: boolean;
    channels?: APIChannel[];
    roles?: APIRole[];
    members?: APIGuildMember[];
    emojis?: APIEmoji[];
    stickers?: APISticker[];
    voice_states?: APIVoiceState[];
    member_count?: number;
    online_count?: number;
    joined_at?: string;
}) | {
    id: Snowflake;
    properties: APIGuild;
    unavailable?: boolean;
    channels?: APIChannel[];
    roles?: APIRole[];
    members?: APIGuildMember[];
    emojis?: APIEmoji[];
    stickers?: APISticker[];
    voice_states?: APIVoiceState[];
    member_count?: number;
    online_count?: number;
    joined_at?: string;
};
/** READY dispatch (op 0, t = READY) — initial connection established. */
interface GatewayReadyDispatchData {
    /** Current user object. */
    user: APIUser;
    /**
     * Guilds for this session.
     * Bot tokens typically receive `[]` here; full snapshots arrive via GUILD_CREATE.
     */
    guilds: GatewayGuildSnapshot[];
    /** Session ID for resuming. */
    session_id: string;
    /** Private / DM channels (user clients; usually absent for bots). */
    private_channels?: APIChannel[];
    /** Shard info [shard_id, num_shards] when sharding. */
    shard?: [number, number];
}
/** MESSAGE_CREATE dispatch — new message. */
type GatewayMessageCreateDispatchData = APIMessage;
/** MESSAGE_UPDATE dispatch — message edited. */
type GatewayMessageUpdateDispatchData = APIMessage;
/** MESSAGE_DELETE dispatch — message deleted. Fluxer may include content/author_id for caching. */
interface GatewayMessageDeleteDispatchData {
    /** Message ID. */
    id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
    /** Message content (Fluxer extension for caching). */
    content?: string | null;
    /** Author ID (Fluxer extension for caching). */
    author_id?: Snowflake | null;
}
/** MESSAGE_DELETE_BULK dispatch — multiple messages deleted. */
interface GatewayMessageDeleteBulkDispatchData {
    /** Message IDs. */
    ids: Snowflake[];
    /** Channel ID. */
    channel_id: Snowflake;
    /** Guild ID (if guild messages). */
    guild_id?: Snowflake;
}
/** Emoji data sent with reaction events (id is null for unicode emoji). */
interface GatewayReactionEmoji {
    /** Emoji ID (null for Unicode emoji). */
    id?: Snowflake;
    /** Emoji name (Unicode char or custom emoji name). */
    name: string;
    /** Whether the emoji is animated. */
    animated?: boolean;
}
/** MESSAGE_REACTION_ADD dispatch — reaction added. */
interface GatewayMessageReactionAddDispatchData {
    /** Message ID. */
    message_id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
    /** User who added the reaction. */
    user_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
    /** Member snapshot (if guild message). */
    member?: APIGuildMember;
    /** Emoji used. */
    emoji: GatewayReactionEmoji;
}
/** Single reaction in MESSAGE_REACTION_ADD_MANY. */
interface GatewayMessageReactionAddManyEntry {
    /** User who added the reaction. */
    user_id: Snowflake;
    /** Emoji used. */
    emoji: GatewayReactionEmoji;
    /** Member snapshot (if guild message). */
    member?: GatewayReactionMemberSnapshot;
}
/** MESSAGE_REACTION_ADD_MANY dispatch — batch reaction adds. */
interface GatewayMessageReactionAddManyDispatchData {
    /** Channel ID. */
    channel_id: Snowflake;
    /** Message ID. */
    message_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
    /** Reactions added. */
    reactions: GatewayMessageReactionAddManyEntry[];
}
/** MESSAGE_REACTION_REMOVE dispatch — reaction removed. */
interface GatewayMessageReactionRemoveDispatchData {
    /** Message ID. */
    message_id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
    /** User who removed the reaction. */
    user_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
    /** Member snapshot (if guild message). */
    member?: APIGuildMember;
    /** Emoji removed. */
    emoji: GatewayReactionEmoji;
}
/** MESSAGE_REACTION_REMOVE_EMOJI dispatch — all reactions of one emoji removed. */
interface GatewayMessageReactionRemoveEmojiDispatchData {
    /** Message ID. */
    message_id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
    /** Emoji removed. */
    emoji: GatewayReactionEmoji;
}
/** MESSAGE_REACTION_REMOVE_ALL dispatch — all reactions removed. */
interface GatewayMessageReactionRemoveAllDispatchData {
    /** Message ID. */
    message_id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
    /** Guild ID (if guild message). */
    guild_id?: Snowflake;
}
/** MESSAGE_ACK dispatch — message acknowledged (read receipt). */
interface GatewayMessageAckDispatchData {
    /** Message ID. */
    message_id: Snowflake;
    /** Channel ID. */
    channel_id: Snowflake;
}
/** GUILD_CREATE — full guild snapshot (nested `properties` or flat). */
type GatewayGuildCreateDispatchData = GatewayGuildSnapshot;
/** GUILD_UPDATE — full guild object */
type GatewayGuildUpdateDispatchData = APIGuild;
/** GUILD_DELETE — id, unavailable? (true = temp outage) */
interface GatewayGuildDeleteDispatchData {
    id: Snowflake;
    unavailable?: boolean;
}
/** CHANNEL_CREATE — full channel */
type GatewayChannelCreateDispatchData = APIChannel;
/** CHANNEL_UPDATE — full channel */
type GatewayChannelUpdateDispatchData = APIChannel;
/** CHANNEL_UPDATE_BULK — channels[] */
interface GatewayChannelUpdateBulkDispatchData {
    channels: APIChannel[];
}
/** CHANNEL_DELETE — full channel */
type GatewayChannelDeleteDispatchData = APIChannel;
/** CHANNEL_RECIPIENT_ADD — channel_id, user (group DM) */
interface GatewayChannelRecipientAddDispatchData {
    channel_id: Snowflake;
    user: APIUser;
}
/** CHANNEL_RECIPIENT_REMOVE — channel_id, user (group DM) */
interface GatewayChannelRecipientRemoveDispatchData {
    channel_id: Snowflake;
    user: APIUser;
}
/** GUILD_MEMBER_ADD — member + guild_id */
type GatewayGuildMemberAddDispatchData = APIGuildMember & {
    guild_id: Snowflake;
};
/** GUILD_MEMBER_UPDATE — guild_id, roles, user, nick?, avatar?, joined_at?, ... */
interface GatewayGuildMemberUpdateDispatchData {
    guild_id: Snowflake;
    roles: Snowflake[];
    user: APIUser;
    nick?: string | null;
    avatar?: string | null;
    joined_at?: string;
    premium_since?: string | null;
    communication_disabled_until?: string | null;
}
/** GUILD_MEMBER_REMOVE — guild_id, user */
interface GatewayGuildMemberRemoveDispatchData {
    guild_id: Snowflake;
    user: APIUser;
}
/** GUILD_MEMBERS_CHUNK — from request_guild_members; members[], chunk_index, chunk_count */
interface GatewayGuildMembersChunkDispatchData {
    guild_id: Snowflake;
    members: Array<APIGuildMember & {
        guild_id?: Snowflake;
    }>;
    chunk_index: number;
    chunk_count: number;
    presences?: Array<{
        user: {
            id: Snowflake;
        };
        status?: string;
        activities?: unknown[];
    }>;
    nonce?: string | null;
}
/** GUILD_MEMBER_LIST_UPDATE — lazy member list; guild_id, id (list_id), member_count, online_count, groups, ops */
interface GatewayGuildMemberListUpdateDispatchData {
    guild_id: Snowflake;
    id: string;
    member_count: number;
    online_count: number;
    groups: Array<{
        id: string;
        count: number;
    }>;
    ops: Array<{
        op: 'SYNC' | 'INVALIDATE' | 'INSERT' | 'UPDATE' | 'DELETE';
        range?: [number, number];
        index?: number;
        item?: GatewayGuildMemberListItem;
        items?: GatewayGuildMemberListItem[];
    }>;
}
/** Item payload used by GUILD_MEMBER_LIST_UPDATE op entries. */
interface GatewayGuildMemberListItem {
    group?: {
        id: string;
        count?: number;
    };
    member?: APIGuildMember & {
        guild_id?: Snowflake;
    };
    user?: APIUser;
    [key: string]: unknown;
}
/** GUILD_BAN_ADD — guild_id, user, reason? */
interface GatewayGuildBanAddDispatchData {
    guild_id: Snowflake;
    user: APIUser;
    reason?: string | null;
}
/** GUILD_BAN_REMOVE — guild_id, user */
interface GatewayGuildBanRemoveDispatchData {
    guild_id: Snowflake;
    user: APIUser;
}
/**
 * INVITE_CREATE — invite payload from gateway.
 * May be partial; some instances send guild_id/channel_id without nested guild/channel objects.
 */
type GatewayInviteCreateDispatchData = Partial<APIInvite> & {
    guild_id?: Snowflake;
    channel_id?: Snowflake;
};
/** INVITE_DELETE — code, channel_id, guild_id? */
interface GatewayInviteDeleteDispatchData {
    code: string;
    channel_id: Snowflake;
    guild_id?: Snowflake;
}
/** TYPING_START — channel_id, user_id, timestamp, guild_id?, member? */
interface GatewayTypingStartDispatchData {
    channel_id: Snowflake;
    user_id: Snowflake;
    timestamp: number;
    guild_id?: Snowflake;
    member?: APIGuildMember & {
        guild_id?: Snowflake;
    };
}
/** USER_UPDATE — full current user (APIUser) */
type GatewayUserUpdateDispatchData = APIUser;
/** GUILD_ROLE_CREATE — guild_id, role */
interface GatewayGuildRoleCreateDispatchData {
    guild_id: Snowflake;
    role: APIRole;
}
/** GUILD_ROLE_UPDATE — guild_id, role */
interface GatewayGuildRoleUpdateDispatchData {
    guild_id: Snowflake;
    role: APIRole;
}
/** GUILD_ROLE_DELETE — guild_id, role_id */
interface GatewayGuildRoleDeleteDispatchData {
    guild_id: Snowflake;
    role_id: Snowflake;
}
/** GUILD_ROLE_UPDATE_BULK — guild_id, roles[] */
interface GatewayGuildRoleUpdateBulkDispatchData {
    guild_id: Snowflake;
    roles: APIRole[];
}
/** VOICE_STATE_UPDATE — full voice state (matches APIVoiceState / VoiceStateResponse). */
type GatewayVoiceStateUpdateDispatchData = APIVoiceState & {
    member?: APIGuildMember & {
        guild_id?: Snowflake;
    };
};
/** Internal sync payload emitted from READY/GUILD_CREATE voice_states (not a gateway dispatch). */
interface GatewayVoiceStatesSyncData {
    guildId: Snowflake;
    voiceStates: APIVoiceState[];
}
/** VOICE_SERVER_UPDATE — token, guild_id, endpoint, connection_id? */
interface GatewayVoiceServerUpdateDispatchData {
    token: string;
    guild_id: Snowflake;
    endpoint: string | null;
    /** Connection ID for subsequent voice state updates (Fluxer). */
    connection_id?: string | null;
}
/** VOICE_STATE_ACK — acknowledgement for a voice state mutation. */
interface GatewayVoiceStateAckDispatchData {
    mutation_id?: string;
    runtime_epoch?: string | null;
    connection_id?: string | null;
    guild_id?: Snowflake | null;
    channel_id?: Snowflake | null;
    status?: string;
    server_version?: number;
    canonical_state?: GatewayVoiceStateUpdateDispatchData | null;
    error_code?: string;
    error_message?: string;
}
/** ENTRANCE_SOUND_PLAY — entrance sound played when a user joins voice. */
interface GatewayEntranceSoundPlayDispatchData {
    user_id: Snowflake;
    channel_id: Snowflake;
    guild_id?: Snowflake | null;
    sound_id: Snowflake;
    hash: string;
    url: string;
    duration_ms: number;
    content_type: string;
}
/** GUILD_EMOJIS_UPDATE — emoji list for a guild changed. */
interface GatewayGuildEmojisUpdateDispatchData {
    guild_id: Snowflake;
    emojis: APIEmoji[];
}
/** GUILD_STICKERS_UPDATE — sticker list for a guild changed. */
interface GatewayGuildStickersUpdateDispatchData {
    guild_id: Snowflake;
    stickers: APISticker[];
}
/** GUILD_AUDIT_LOG_ENTRY_CREATE — new audit log entry. */
interface GatewayGuildAuditLogEntryCreateDispatchData extends APIGuildAuditLogEntry {
    guild_id?: Snowflake;
}
/** GUILD_COUNTS_UPDATE — member/online counts for guilds. */
interface GatewayGuildCountEntry {
    guild_id: Snowflake;
    member_count: number;
    online_count: number;
}
interface GatewayGuildCountsUpdateDispatchData {
    counts?: GatewayGuildCountEntry[];
}
/** CHANNEL_MEMBER_COUNTS_UPDATE — member/online counts for channels. */
interface GatewayChannelMemberCountEntry {
    guild_id: Snowflake;
    channel_id: Snowflake;
    member_count: number;
    online_count: number;
}
interface GatewayChannelMemberCountsUpdateDispatchData {
    counts?: GatewayChannelMemberCountEntry[];
}
/** CHANNEL_PINS_UPDATE — pins in a channel changed. */
interface GatewayChannelPinsUpdateDispatchData {
    guild_id?: Snowflake;
    channel_id: Snowflake;
    last_pin_timestamp?: string | null;
}
/** CHANNEL_PINS_ACK — user acknowledged viewing pinned messages */
interface GatewayChannelPinsAckDispatchData {
    channel_id: Snowflake;
    last_pin_timestamp?: string | null;
}
/** PRESENCE_UPDATE — user presence (status, activities) changed. */
interface GatewayPresenceUpdateDispatchData {
    user: {
        id: Snowflake;
    };
    guild_id?: Snowflake;
    status?: string;
    activities?: Array<{
        name: string;
        /** Activity type integer (no closed OpenAPI enum yet). */
        type: number;
        url?: string | null;
    }>;
    /** Custom status (Fluxer). */
    custom_status?: GatewayCustomStatus | null;
}
/** PRESENCE_UPDATE_BULK — multiple presence updates. */
interface GatewayPresenceUpdateBulkDispatchData {
    presences: GatewayPresenceUpdateDispatchData[];
    guild_id?: Snowflake;
}
/** WEBHOOKS_UPDATE — webhooks in a channel were updated. */
interface GatewayWebhooksUpdateDispatchData {
    guild_id: Snowflake;
    channel_id: Snowflake;
}
/** RESUMED — connection resumed; typically no payload. */
type GatewayResumedDispatchData = undefined;
/** SESSIONS_REPLACE — user's active sessions list replaced */
interface GatewaySessionsReplaceDispatchData {
    sessions?: GatewaySession[];
}
/** USER_SETTINGS_UPDATE — user settings (theme, locale, etc.) changed */
interface GatewayUserSettingsUpdateDispatchData {
    locale?: string;
    theme?: string;
    status?: string;
    custom_status?: GatewayCustomStatus | null;
    [key: string]: unknown;
}
/** USER_GUILD_SETTINGS_UPDATE — per-guild settings changed */
interface GatewayUserGuildSettingsUpdateDispatchData {
    guild_id?: Snowflake;
    channel_overrides?: Array<{
        channel_id: Snowflake;
        muted?: boolean;
        [key: string]: unknown;
    }>;
    muted?: boolean;
    [key: string]: unknown;
}
/** USER_CONNECTIONS_UPDATE — linked external connections changed. */
type GatewayUserConnectionsUpdateDispatchData = APIUserConnectionsUpdate;
/** WEBAUTHN_CREDENTIALS_UPDATE — WebAuthn credential list replaced. */
type GatewayWebAuthnCredentialsUpdateDispatchData = APIWebAuthnCredential[];
/** USER_PINNED_DMS_UPDATE — pinned DM order changed */
interface GatewayUserPinnedDmsUpdateDispatchData {
    pinned_channels?: Snowflake[];
    [key: string]: unknown;
}
/** USER_NOTE_UPDATE — note on another user changed */
interface GatewayUserNoteUpdateDispatchData {
    id: Snowflake;
    note?: string | null;
}
/** RECENT_MENTION_DELETE — recent mention cleared */
interface GatewayRecentMentionDeleteDispatchData {
    id?: Snowflake;
    channel_id?: Snowflake;
    guild_id?: Snowflake;
    [key: string]: unknown;
}
/** SAVED_MESSAGE_CREATE — message saved (bookmarked) */
type GatewaySavedMessageCreateDispatchData = APIMessage;
/** SAVED_MESSAGE_DELETE — saved message unsaved */
interface GatewaySavedMessageDeleteDispatchData {
    id: Snowflake;
}
/** AUTH_SESSION_CHANGE — login/logout on another client */
interface GatewayAuthSessionChangeDispatchData {
    session_id?: string;
    kind?: 'login' | 'logout' | 'update' | string;
    [key: string]: unknown;
}
/** PASSIVE_UPDATES — lazy-loaded entity updates */
interface GatewayPassiveUpdatesDispatchData {
    guild_id?: Snowflake;
    [key: string]: unknown;
}
/** GUILD_SYNC — guild sync state (passive/lazy) */
interface GatewayGuildSyncDispatchData {
    id?: Snowflake;
    guild_id?: Snowflake;
    [key: string]: unknown;
}
/** RELATIONSHIP_ADD — relationship (friend, block) added */
interface GatewayRelationshipAddDispatchData {
    id: Snowflake;
    type: RelationshipType;
}
/** RELATIONSHIP_UPDATE — relationship updated */
interface GatewayRelationshipUpdateDispatchData {
    id: Snowflake;
    type: RelationshipType;
}
/** RELATIONSHIP_REMOVE — relationship removed */
interface GatewayRelationshipRemoveDispatchData {
    id: Snowflake;
}
/** CALL_CREATE — call created */
interface GatewayCallCreateDispatchData {
    id: Snowflake;
    channel_id: Snowflake;
    [key: string]: unknown;
}
/** CALL_UPDATE — call updated */
interface GatewayCallUpdateDispatchData {
    id: Snowflake;
    channel_id: Snowflake;
    [key: string]: unknown;
}
/** CALL_DELETE — call ended */
interface GatewayCallDeleteDispatchData {
    id: Snowflake;
    channel_id: Snowflake;
}
/** FAVORITE_MEME_CREATE — favorite meme/media added */
type GatewayFavoriteMemeCreateDispatchData = GatewayFavoriteMemePayload;
/** FAVORITE_MEME_UPDATE — favorite meme/media updated */
type GatewayFavoriteMemeUpdateDispatchData = GatewayFavoriteMemePayload;
/** FAVORITE_MEME_DELETE — favorite meme/media removed */
type GatewayFavoriteMemeDeleteDispatchData = GatewayFavoriteMemePayload;
/** Active session object used by SESSIONS_REPLACE. */
interface GatewaySession {
    session_id?: string;
    status?: string;
    activities?: Array<{
        name?: string;
        type?: number;
        [key: string]: unknown;
    }>;
    client_info?: {
        os?: string;
        client?: string;
        version?: number;
        [key: string]: unknown;
    };
    [key: string]: unknown;
}
/** Shared favorite meme/media payload shape. */
interface GatewayFavoriteMemePayload {
    id?: Snowflake;
    user_id?: Snowflake;
    name?: string;
    url?: string;
    [key: string]: unknown;
}
interface GatewayReceivePayload<T = unknown> {
    op: GatewayOpcodes;
    d?: T;
    s?: number;
    t?: GatewayDispatchEventName;
}

export { type APIAllowedMentions, type APIApplicationMe, type APIBan, type APIBulkMessageFetchRequest, type APIBulkMessageFetchRequestItem, type APIBulkMessageFetchResponse, type APIBulkMessageFetchResponseChannel, type APIChannel, type APIChannelOverwrite, type APIChannelPartial, type APIChannelPinItem, type APIChannelPinsPage, type APIChannelSlowmodeState, type APIConnectedAccount, type APIDiscoveryApplication, type APIDiscoveryStatus, type APIEmbed, type APIEmbedAuthor, type APIEmbedChild, type APIEmbedField, type APIEmbedFooter, type APIEmbedMedia, type APIEmoji, type APIEmojiMetadata, type APIEmojiWithUser, type APIErrorBody, APIErrorCode, type APIGatewayBotResponse, type APIGroupDmInvite, type APIGroupDmInviteMetadata, type APIGuild, type APIGuildAuditLog, type APIGuildAuditLogEntry, type APIGuildEmojiBulkCreateResponse, type APIGuildFeatureToggle, type APIGuildInvite, type APIGuildInviteMetadata, type APIGuildMember, type APIGuildMemberSearchRequest, type APIGuildMemberSearchResponse, type APIGuildMemberSearchResult, type APIGuildPartial, type APIGuildStickerBulkCreateResponse, type APIInstance, type APIInstanceAppPublic, type APIInstanceCaptcha, type APIInstanceCommunity, type APIInstanceEndpoints, type APIInstanceFeatures, type APIInstanceGif, type APIInstanceLimitRule, type APIInstanceLimits, type APIInstancePush, type APIInstanceRegistration, type APIInstanceServices, type APIInstanceSso, type APIInvite, type APIInviteMetadata, type APIInviteResponse, type APIMessage, type APIMessageAttachment, type APIMessageCall, type APIMessageReaction, type APIMessageReference, type APIMessageSnapshot, type APIMessageSticker, type APIOAuthApplication, type APIPackCreateRequest, type APIPackDashboard, type APIPackDashboardSection, type APIPackInvite, type APIPackInviteInfo, type APIPackInviteMetadata, type APIPackSummary, type APIPackUpdateRequest, type APIPreloadMessagesResponse, type APIProfileResponse, type APIReactionEmoji, type APIRole, type APIRtcRegion, type APISticker, type APIStickerMetadata, type APIStickerWithUser, type APISudoVerification, type APIUser, type APIUserConnection, type APIUserConnectionsUpdate, type APIUserPartial, type APIUserProfile, type APIUserTagCheck, type APIVanityURL, type APIVoiceState, type APIVoiceStateStub, type APIWebAuthnCredential, type APIWebhook, type APIWebhookEditMessageRequest, type APIWebhookTokenUpdateRequest, type APIWebhookUpdateRequest, type APIWellKnownFluxer, type AllowedMentionType, AuditLogActionType, type ChannelCreateRequest, type ChannelInviteCreateRequest, ChannelType, ContentWarningLevel, DefaultMessageNotifications, EmbedMediaFlags, type EmbedType, type GatewayAuthSessionChangeDispatchData, type GatewayCallCreateDispatchData, type GatewayCallDeleteDispatchData, type GatewayCallUpdateDispatchData, type GatewayChannelCreateDispatchData, type GatewayChannelDeleteDispatchData, type GatewayChannelMemberCountEntry, type GatewayChannelMemberCountsUpdateDispatchData, type GatewayChannelPinsAckDispatchData, type GatewayChannelPinsUpdateDispatchData, type GatewayChannelRecipientAddDispatchData, type GatewayChannelRecipientRemoveDispatchData, type GatewayChannelUpdateBulkDispatchData, type GatewayChannelUpdateDispatchData, type GatewayCustomStatus, type GatewayDispatchEventName, GatewayDispatchEvents, type GatewayEntranceSoundPlayDispatchData, type GatewayErrorCode, GatewayErrorCodes, type GatewayFavoriteMemeCreateDispatchData, type GatewayFavoriteMemeDeleteDispatchData, type GatewayFavoriteMemePayload, type GatewayFavoriteMemeUpdateDispatchData, type GatewayGuildAuditLogEntryCreateDispatchData, type GatewayGuildBanAddDispatchData, type GatewayGuildBanRemoveDispatchData, type GatewayGuildCountEntry, type GatewayGuildCountsUpdateDispatchData, type GatewayGuildCreateDispatchData, type GatewayGuildDeleteDispatchData, type GatewayGuildEmojisUpdateDispatchData, type GatewayGuildMemberAddDispatchData, type GatewayGuildMemberListItem, type GatewayGuildMemberListUpdateDispatchData, type GatewayGuildMemberRemoveDispatchData, type GatewayGuildMemberUpdateDispatchData, type GatewayGuildMembersChunkDispatchData, type GatewayGuildRoleCreateDispatchData, type GatewayGuildRoleDeleteDispatchData, type GatewayGuildRoleUpdateBulkDispatchData, type GatewayGuildRoleUpdateDispatchData, type GatewayGuildSnapshot, type GatewayGuildStickersUpdateDispatchData, type GatewayGuildSyncDispatchData, type GatewayGuildUpdateDispatchData, type GatewayHelloData, type GatewayIdentifyData, GatewayIdentifyFlags, type GatewayInviteCreateDispatchData, type GatewayInviteDeleteDispatchData, type GatewayMessageAckDispatchData, type GatewayMessageCreateDispatchData, type GatewayMessageDeleteBulkDispatchData, type GatewayMessageDeleteDispatchData, type GatewayMessageReactionAddDispatchData, type GatewayMessageReactionAddManyDispatchData, type GatewayMessageReactionAddManyEntry, type GatewayMessageReactionRemoveAllDispatchData, type GatewayMessageReactionRemoveDispatchData, type GatewayMessageReactionRemoveEmojiDispatchData, type GatewayMessageUpdateDispatchData, GatewayOpcodes, type GatewayPassiveUpdatesDispatchData, type GatewayPresenceUpdateBulkDispatchData, type GatewayPresenceUpdateData, type GatewayPresenceUpdateDispatchData, type GatewayReactionEmoji, type GatewayReactionMemberSnapshot, type GatewayReadyDispatchData, type GatewayReceivePayload, type GatewayRecentMentionDeleteDispatchData, type GatewayRelationshipAddDispatchData, type GatewayRelationshipRemoveDispatchData, type GatewayRelationshipUpdateDispatchData, type GatewayRequestChannelMemberCountsData, type GatewayRequestGuildCountsData, type GatewayRequestGuildMembersData, type GatewayResumeData, type GatewayResumedDispatchData, type GatewaySavedMessageCreateDispatchData, type GatewaySavedMessageDeleteDispatchData, type GatewaySendPayload, type GatewaySession, type GatewaySessionsReplaceDispatchData, type GatewayTypingStartDispatchData, type GatewayUserConnectionsUpdateDispatchData, type GatewayUserGuildSettingsUpdateDispatchData, type GatewayUserNoteUpdateDispatchData, type GatewayUserPinnedDmsUpdateDispatchData, type GatewayUserSettingsUpdateDispatchData, type GatewayUserUpdateDispatchData, type GatewayVoiceServerUpdateDispatchData, type GatewayVoiceStateAckDispatchData, type GatewayVoiceStateUpdateData, type GatewayVoiceStateUpdateDispatchData, type GatewayVoiceStatesSyncData, type GatewayWebAuthnCredentialsUpdateDispatchData, type GatewayWebhooksUpdateDispatchData, type GuildCategoryChannelCreateRequest, GuildExplicitContentFilter, type GuildFeature, type GuildLinkChannelCreateRequest, GuildMFALevel, GuildMemberProfileFlags, type GuildMemberProfileFlagsValue, GuildNSFWLevel, GuildOperations, type GuildOperationsValue, type GuildTextChannelCreateRequest, GuildVerificationLevel, type GuildVoiceChannelCreateRequest, InviteType, MessageAttachmentFlags, MessageFlags, type MessageFlagsValue, MessageReferenceType, MessageType, OverwriteType, type PackInviteCreateRequest, type PackType, PublicUserFlags, type PublicUserFlagsValue, type RESTCreateRoleBody, type RESTPatchAPIGuildDiscoveryJSONBody, type RESTPatchAPIGuildEmojiJSONBody, type RESTPatchAPIGuildStickerJSONBody, type RESTPostAPIChannelAttachmentCompleteItem, type RESTPostAPIChannelAttachmentCompleteRequest, type RESTPostAPIChannelAttachmentCompleteResponse, type RESTPostAPIChannelAttachmentCompleteResult, type RESTPostAPIChannelAttachmentUploadItem, type RESTPostAPIChannelAttachmentUploadMultipart, type RESTPostAPIChannelAttachmentUploadMultipartPart, type RESTPostAPIChannelAttachmentUploadRequest, type RESTPostAPIChannelAttachmentUploadResponse, type RESTPostAPIChannelAttachmentUploadResponseItem, type RESTPostAPIChannelAttachmentUploadSinglepart, type RESTPostAPIEmbed, type RESTPostAPIEmbedAuthor, type RESTPostAPIEmbedFooter, type RESTPostAPIEmbedMedia, type RESTPostAPIGuildDiscoveryJSONBody, type RESTPostAPIGuildEmojiJSONBody, type RESTPostAPIGuildStickerJSONBody, type RESTPostAPIMessageAttachment, type RESTPostAPIMessageAttachmentMeta, type RESTPostAPIMessageUploadedAttachment, type RESTPostAPIPreloadMessagesJSONBody, type RESTUpdateRoleBody, type RateLimitErrorBody, RelationshipType, Snowflake, SplashCardAlignment, SystemChannelFlags, type SystemChannelFlagsValue, WebhookType, isGroupDmInvite, isGuildInvite, isPackInvite, isUploadedAttachment };
